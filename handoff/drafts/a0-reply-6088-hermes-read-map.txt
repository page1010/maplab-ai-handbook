【Claude・Fable5 線・同 session 續接】

指路卡寫好了,檔名 HERMES_READ_MAP.md,已同時登錄進 SYSTEM_DIRECTORY_INDEX 的冷啟動順序和 AGENT_RULES 的 hermes 章節,冷啟動繞不過去。

先講一件實查出來的事:沒有人在權限上擋 hermes。

全 repo 搜過六種讀取白名單寫法,一條都沒有。唯一叫 allowlist 的是 deerflow 那三個 yaml,那是對外研究的網域護欄,跟讀本機檔案無關。SYSTEM_DIRECTORY_INDEX 共 625 行,提到 Hermes 的只有 3 行,而且那 3 行全部是把 Hermes 列進適用名單。

所以真相是:hermes 一直被列在適用名單裡,卻從來沒有人寫過一張 hermes 尺寸的路線卡。625 行的全角色索引是地圖集,不是路線。這是指路沒做,不是權限沒開。

=== 卡裡有什麼 ===

一,企業文化讀順序 12 檔,附行數合計 3622 行,逐檔在磁碟上核對過存在。時間只夠讀三檔就讀 AGENT_CORE、docs/company-values、SOP_SEE_BEFORE_YOU_SAY。

二,資料位置 17 列,每一列標了看到還是查到。家目錄 .maplab 底下那三列誠實標查到,因為這次列目錄被工作目錄限制擋下,失敗原文照貼在卡裡。hermes 讀得到就自己升級成看到並回寫。

三,避開清單。唯一新紅線就是股票的程式單:不讀不改不執行下單路徑、不下單、不動倉位。交易紀錄的唯讀查詢可以做,產出只能是報表不能是指令。

金鑰、他人客資、兒童照、夾帶權杖的網址這四類不在放寬範圍,跟階段無關,永久禁止。

四,卡住怎麼辦三段式:失敗原文照貼、說出卡在哪一層、給一個解鎖動作。讀不到不等於不存在,也不等於可以推論。

=== 額度那句的正解 ===

額度不夠是因為吃 token 的活沒外包。卡裡列了五件今天就能派的唯讀工作,全部不碰交易:

1. pitfalls.md 941 行做成分類索引,以後查坑讀索引不讀全文
2. AGENT_RULES.md 1316 行做成可搜尋的規則目錄,現在沒目錄所以每次全讀
3. task_index.json 和 handoff/tasks 實際檔案對差集
4. decisions.md 找出已定案但程式裡還沒寫死的條目
5. 把家目錄 .maplab 那三列從查到升級成看到

前兩件最省額度。那兩份合計 2257 行,現在每個 agent 開工都要全吞一次。

=== 誠實項 ===

一,這輪開放的是讀取和產出文字,不是階段升級。S2 端到端還是沒驗過,S3 照舊不開,門檻沒動。

二,指路卡是文字,還沒有 hermes 真的照它跑過一輪。要驗證就派上面第 1 或第 2 件,做出來的索引能不能用一看就知道。

三,家目錄 .maplab 三列停在查到等級,那是這個視窗的工作目錄限制,不是 hermes 的限制。
