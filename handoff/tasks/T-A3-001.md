# T-A3-001 — GTM LINE 按鈕追蹤完整方案

---

## 接續狀態

> **Agent 冷啟動時第一個看的區塊。每次 checkpoint 必須更新。**

- **狀態**: ⏸️ 阻塞中
- **最後活動**: 2026-03-29
- **接續點**: 方案A ✅ 完成（GTM v19 驗證）。方案B 待 GTM 後台手動新增 Click ID trigger。
- **阻塞**: 需 Owner 登入 GTM 後台操作（A1 無法遠端操作 GTM UI）

---

## Meta
- **Task ID**: T-A3-001
- **負責 Agent**: A3（SEO & Ads Team）
- **建立日期**: 2026-03-29
- **最後更新**: 2026-04-11（A1 格式統一）

---

## 現況摘要

### GTM 目前狀態（v19，已驗證）

| 代碼 | 觸發條件 | 狀態 |
|------|---------|------|
| Meta - LINE Click Event | 僅連結 regex `(lihi2\.com\|lin\.ee\|line\.me)` | ✅ 已驗證（Owner Pixel Helper 確認） |
| Google Ads 轉換追蹤+么=line點整url | 同上 regex | ✅ 已部署 |
| Google Ads 轉換追蹤-左下角line button | CSS selector `a[href*="lin.ee"]` | ✅ 已部署 |

**結論：方案 A（Regex URL 觸發）已在 GTM v19 完整實作並由 Owner 驗證。**

---

## 網站 LINE 按鈕 HTML（2026-03-29 實測）

WP REST API 讀取首頁（Page ID: 1250）確認：

```html
<a class="elementor-button elementor-button-link elementor-size-sm"
   href="https://lin.ee/IP8nt4n"
   id="LDP轉line"
   rel="nofollow noopener"
   target="_blank">
  <span class="elementor-button-text">💬 LINE 免費諮詢</span>
</a>
```

**關鍵發現：**
- 所有首頁 LINE CTA 按鈕都有 `id="LDP轉line"`（多個按鈕共用同一 id）
- URL 統一為 `https://lin.ee/IP8nt4n`
- 已被 GTM v19 的 regex trigger 捕獲 ✅

---

## 方案 B — Click Element ID 觸發（待執行）

**方案 B** 使用 `Click Element ID` 而非 URL 比對，更能捕捉到所有 id="LDP轉line" 的按鈕點擊（包含未來新增的 LINE CTA）。

### GTM 設定規格

#### 觸發條件
| 欄位 | 值 |
|------|-----|
| 觸發條件類型 | 點擊 — 所有元素 |
| 觸發條件名稱 | `Trigger - LINE Button ID` |
| 觸發時機 | 部分點擊 |
| 條件 | `Click ID` 包含 `LDP轉line` |

#### Meta 標籤（補充型）
| 欄位 | 值 |
|------|-----|
| 標籤類型 | 自訂 HTML |
| 標籤名稱 | `Meta - LINE Button ID Click` |
| HTML | `<script>fbq('track', 'Contact', {content_name: 'LINE Button - ID Click'});</script>` |
| 觸發條件 | `Trigger - LINE Button ID` |

> 注意：方案 A 已涵蓋主要場景。方案 B 作為補充，適合追蹤 A/B 測試或未來新增的 LINE CTA。

---

## 執行狀態

| 項目 | 狀態 | 備註 |
|------|------|------|
| 方案 A regex trigger | ✅ GTM v19 完成驗證 | Owner 確認 Meta Pixel Contact 事件觸發 |
| 方案 B ID trigger 設定 | 🔲 待執行 | 需 GTM 後台登入，按上述規格新增 |
| GTM 發布 | 🔲 待方案B完成後 | 版本名稱建議：`方案B — LINE ID 觸發補充` |
| Pixel Helper 驗證 | 🔲 待方案B發布後 | 點擊 LINE 按鈕確認 Contact 事件 |

---

## 斷點紀錄（2026-03-29）

**方案 B 需要登入 GTM 後台手動設定，A1 無法遠端操作 GTM UI。**

**下次接手步驟：**
1. 登入 https://tagmanager.google.com/#/container/accounts/6000782046/containers/30897681
2. 新增觸發條件：`Trigger - LINE Button ID`（Click ID 包含 LDP轉line）
3. 新增代碼：`Meta - LINE Button ID Click`
4. 提交新版本並發布
5. 用 Pixel Helper 驗證

---

## 版本紀錄

| 版本 | 日期 | 說明 | 執行者 |
|------|------|------|--------|
| v1.0 | 2026-03-29 | 初始版本：方案A狀態確認 + 方案B設定規格 + 網站HTML實測結果 | A1 Claude Code |
