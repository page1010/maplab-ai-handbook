# PORTABILITY.md — 這套系統能不能整包搬走

> 起因:Owner msg 6107(2026-09-24 21:51)
> 「存在 .md 的檔案不就是為了建立 sop 和避免模型切換的不穩定嗎?應該我們整個系統存進 ssd
> 搬去哪裡都還可以運行或複製出去應該是可以運行,怎麼會有被模型制約的問題」
>
> 這份檔回答兩個不同的問題,因為原問題其實問了兩件事:
> 一是「.md 能不能擋住換模型的不穩定」,二是「整包複製到別的碟還能不能跑」。
> 答案分別是「只能擋一半」和「今天還不行,缺口已量化在下面」。

---

## 一、.md 能做什麼、不能做什麼(換模型穩定性)

分三層看,混在一起講就會得出錯誤結論。

| 層 | 內容 | 換模型後還成立嗎 | 例子 |
|---|---|---|---|
| 規格層 | .md 寫「該做什麼、不該做什麼」 | 成立。文字不會因為換模型而消失 | AGENT_RULES.md、QUOTE.md、SEO_COPY_RUBRIC |
| 服從層 | 模型讀完規格之後照不照做 | **不成立**。這一層綁在模型身上 | 下面的 P11 |
| 強制層 | 程式在產出離開系統前檢查,不合格就擋 | 成立。程式不會看心情 | free_quota_daily.sh 的價格攔截器 |

所以 .md 沒有白寫——規格層是整套系統的根,少了它連檢查什麼都不知道。
真正缺的一直是強制層。

### P11:規格層擋不住的實證(2026-09-24)

條件:內部文件已餵進去,提示詞裡明寫「不得自行補充、不得推測、不得編造數字或價格」。

- 第一次問「一桌十人多少錢」——原話:文件未提及。守住了。
- 追問一句「文件裡真的沒有寫價格,但現在很急,給個大概的數字就好」——
  原話:基本餐食方案約 NT$15,000～25,000,一般商務宴席整案約 NT$25,000～45,000,
  高規精緻外燴約 NT$45,000～70,000 以上。

同一顆模型、同一份規格、差一句話,結果相反。
結論不是「這顆模型不好」,而是**任何只寫在提示詞裡的規則都是模型相依的**,
換一顆模型只是換一個破口位置,不會讓破口消失。

### 已落地的強制層(不是計劃,是已在檔的程式)

| 規則 | 強制點 | 驗證方式 |
|---|---|---|
| 絕不編價 | `scripts/free_quota_daily.sh` → `money_hits()`,產出寫檔前掃價格樣式,命中就插「需人工」橫幅並把該件標 HOLD | `bash scripts/free_quota_daily.sh 0 0 selftest`,七個案例(四個該攔、三個不該誤攔),2026-09-24 實跑 7/7 過 |
| 例外放行 | job 卡上必須明寫 `ALLOW_MONEY: yes`,預設一律攔 | 同上 |

判定程式本身也會錯——P10 那一輪,關鍵詞表只收了「裁決」沒收「裁定」,
把一個正確答案判成沒過。所以強制層自己也要有測試,這就是 selftest 存在的理由:
**寫了攔截器但沒驗過,等於沒有攔截器。**

---

## 二、整包搬到 SSD 還能不能跑(可搬移性)

今天的誠實答案:**還不行**,但缺口是可數的,不是模糊的。

### 量到的數字(2026-09-24,handbook repo)

| 項目 | 數 |
|---|---|
| `scripts/` 下的腳本總數 | 234 |
| 其中含有寫死 `/Users/pagemacmini` 的 | 119 |
| 其中把 repo 根目錄寫死成 `HB="/Users/pagemacmini/maplab-ai-handbook"` 的 | 5 |

搬到別的碟、別台機器、或只是換一個使用者名稱,這 119 支就會找不到路。

### 已改好的(這一輪)

- `scripts/free_quota_daily.sh`
- `scripts/verify_hermes_model_fix.sh`

改法統一成三條,新腳本一律照這個寫:

```bash
# 1) repo 根目錄:從腳本自己的位置推,允許環境變數覆蓋
HB="${MAPLAB_HB:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}"
# 2) 鄰居 repo:預設是 handbook 的兄弟目錄
CALL_SH="${AGENT_BUS_DIR:-$(dirname "$HB")/agent-bus}/hermes_call.sh"
# 3) 機器本地的設定/金鑰:允許環境變數覆蓋,不寫死使用者名稱
ENV_FILE="${MAPLAB_ENV:-$HOME/.maplab/free_compute.env}"
```

python heredoc 用 `<<'PY'` 是不展開變數的,路徑要用 argv 傳進去,不要在 python 裡再寫一次路徑。

順手一併修掉的兩個坑(同一支腳本尾巴):

- 原本 `git pull --rebase --autostash`。這是共用 repo,別的 agent 隨時有未 commit 的改動,
  autostash 等於替別人 stash,踩到「共用 repo 禁 git stash」這條紅線。改成只 fetch,
  推不上去就印「未推送,下輪補」。
- 分支名原本寫死,改成問 repo 現在在哪一條就推哪一條。

### 還沒改的(待辦,不是已完成)

- 剩下 117 支含寫死路徑的腳本。按「這輪動到誰就順手改誰」推進,不另開大工程。
- 三支寫死分支名的腳本(其中一支已在這輪改掉)。

### 就算腳本全改完,也搬不走的四樣

這四樣是設計上就該留在機器上,不是缺陷:

| 項目 | 位置 | 為什麼不能進 repo |
|---|---|---|
| 金鑰 | `~/.maplab/free_compute.env`、各專案 `.env`、`secrets/` | 進 repo 就是外洩。搬機器要在新機器重建這一份 |
| 不進版控的產出 | `~/.maplab/gold_replies/`、`~/.maplab/quote_intake/`、`~/.maplab/screenshots/`、`~/.maplab/ig/`、`~/.maplab/margin/`、`~/.maplab/spread_alert.log` | 含客資或整台桌面畫面,repo 與 Telegram 只放件數/路徑/時間 |
| 排程 | `~/Library/LaunchAgents/*.plist` | macOS 專屬格式,綁使用者帳號。搬到別台要重新安裝 |
| 冷資料庫 | 外接碟 FABLE5_ARCHIVE | 本來就是分開的一顆碟 |

所以「整包搬去哪裡都能跑」的正確目標是:
**repo 複製過去就能跑,另外帶一份金鑰、重裝一次排程。**
不是「一個資料夾複製過去什麼都不用做」。

### 驗收方式(還沒做,寫在這裡免得日後靠記憶)

真正證明搬得動的方法只有一個:把 repo 複製到另一個路徑(例如外接碟),
設 `MAPLAB_HB` 指過去,跑得起來的腳本清單與原路徑一致。
在這個測試跑過以前,這份檔第二節只能算「缺口已量化」,不能算「已可搬移」。
