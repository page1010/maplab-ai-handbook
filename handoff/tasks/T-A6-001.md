# T-A6-001 — A6 LINE 業務報價助手系統

---

## 接續狀態

> **Agent 冷啟動時第一個看的區塊。每次 checkpoint 必須更新。**

- **狀態**: 🔄 進行中
- **最後活動**: 2026-07-07 a6-telegram-takeover-route-guard
- **接續點**: Case Store v0 已接到現有 `CONVERSATION_LOG`；A6 Telegram 新增 `/linecases`、`/case`、`/casequote`。2026-05-19 修正 route guard 並把一般聊天/SEO 改成 Codex-first：`/start` 預設一般聊天，`/status`/`/model`/「你現在是跑什麼模型」直接回 A6 runtime/model 狀態；普通對話先走 `codex exec --ephemeral --sandbox read-only`，Codex 不可用才透明 fallback 到 Ollama；只有明確「報價 ...」、`/localquote ...` 或可判定為外燴報價需求才進 A5。2026-06-18 修正 A5 報價 hot path：明確報價文字先走 deterministic `build_sheet_quote_payload()` → GAS `createQuoteVariants` → Google Sheet URL，成功後回讀菜單/總額/成本/毛利；雲端或本地模型只作 fallback，`/localquote` 明確不寫 Sheet。GAS Web App 已 redeploy 到既有 A6 deployment `@12`，修掉 D19/D20 禮盒殘留。驗證：`workbook/reviews/A6-QUOTE-SHEET-FIRST-20260618/validation_report.md`。同日用 OpenClaw 做 A5/A6 報價學徒訓練，Round 1/2 皆自判 PASS 但主管校正為 NEEDS_CORRECTION；已落 `workbook/reviews/A6-QUOTE-OPENCLAW-TRAINING-20260618/supervisor_lesson.md`，下次不得接受下游 agent 自判 PASS，需依 `skills/a5-quotation-engine-skills.md` 學徒 gate 檢查。下一步：重啟/確認 `com.maplab.a6bot` 後 Telegram 實機跑 `/status`、`15人有主食高毛利 要英文菜單`、caption 報價照片、`/localquote ...`、`/linecases today`、`/case Penny`、`/casequote Penny`。
- **2026-07-07 checkpoint**: A6 Telegram 已新增 `/takeover` 接手包；長句提到 `ollama`/模型不再誤回 `/status`；非報價圖片不再走舊 Claude CLI 裸錯；`/localquote` fallback 已在 Chrome Telegram readback 顯示手機可讀摘要。Receipt: `workbook/reviews/A6-TELEGRAM-TAKEOVER-20260707/validation_report.md`。
- **阻塞**: LINE webhook 已可看到 inbound 同步，但它只含客戶→OA 訊息；若要完整雙向訓練資料，需 LINE OA Manager CSV 匯出或其他正式來源。

---

**建立**：2026-04-01 | **負責**：A6 | **目標**：LINE OA 對話自動寫入 CONVERSATION_LOG → 觸發報價草稿

---

## 目標

LINE OA 收到客戶詢問 → Apps Script Webhook → 寫入 Google Sheet CONVERSATION_LOG → A6 讀取 → 輸出報價草稿

---

## 已完成

| Commit | 日期 | 內容 |
|--------|------|------|
| `c6e1f70` | 03-26 | 急件報價 SOP v1.0（6 筆歷史訂單建模板） |
| `d9fba1a` | 03-29 | 系統規劃 v1.0 落地 |
| `3a2df7b` | 03-29 | **v1.1**：三層資料架構（SALES_INTAKE / REVISION_LOG / CONVERSATION_LOG）+ A6 skill 技能書 |
| `3e00f66` | 04-01 | LINE Bot Webhook 技能書（channel secret / token / Apps Script URL 存檔） |
| `local-ollama-mode-routing` | 05-04 | Telegram 模式選單（報價/聊天/SEO）+ Ollama 本地聊天/SEO 路由 + 保留原 Claude+GAS 報價流程 |
| `case-store-v0-routing` | 05-19 | Case Store v0：只讀 CONVERSATION_LOG → 本機 SQLite 案件索引；A6 Telegram 新增 /linecases /case /casequote |
| `a6-route-guard-status` | 05-19 | A6 預設一般聊天；新增 /status /model；模型/status/一般問題不再誤入 A5 報價 fallback |
| `a6-codex-first-chat` | 05-19 | A6 一般聊天/SEO 先走 Codex read-only ephemeral；Codex 不可用才 fallback 到本機 Ollama |

**Sheet 結構已定義**：`SALES_INTAKE` / `REVISION_LOG` / `CONVERSATION_LOG` 三分頁設計完成。

---

## 現在卡在哪裡（2026-04-01 斷點）

**舊斷點：CONVERSATION_LOG 只有標題列，空白。Webhook 未通。**

**2026-05-19 更新：此段已過期。** 目前 Sheet `CONVERSATION_LOG` 可看到 2026-05-19 同日 LINE inbound 訊息，表示 webhook 或等效同步路徑已在寫入。限制仍在：`case_id` 多數空白，且 webhook 只能記錄客戶傳入 OA 的訊息，無法捕捉我方業務回覆。

### 根本原因

LINE OA Manager 的 Webhook 開關已開，但 **LINE Developers Console 的 Webhook URL 欄位尚未填入**。

### 需要 Owner 操作（5 分鐘內可解）

1. 打開 [LINE Developers Console → Channel 1654658337 → Messaging API](https://developers.line.biz/console/channel/1654658337/basics)（點上方「Messaging API」頁籤）
2. 找到 **「Webhook URL」** 欄位，貼入：
   ```
   https://script.google.com/macros/s/AKfycbz_zA_tG2fxNRlvrRMsJyMAzbnpNC-IL8oKqc5h94kyhExsIOuuo7LujbrSuZGK_eap/exec
   ```
3. 點 **「Update」** → 點 **「Verify」**
4. 看到 `200 OK` → Webhook 通了

### 驗證方法

LINE 傳一則訊息給 OA → 看 CONVERSATION_LOG 是否新增一列。

---

## 待做（Webhook 通了之後）

| 優先 | 任務 | 說明 |
|------|------|------|
| P1 | Apps Script 解析訊息格式 | 把 LINE 事件 JSON 拆解成 Sheet 欄位 |
| P2 | QUOTE_WORKBENCH 報價草稿生成 | A6 讀 CONVERSATION_LOG → 呼叫 A5 產出草稿 |
| P3 | REVISION_LOG 修改追蹤 | 業務修改後寫回差異，學習用 |

---

## Apps Script 資訊

- **部署 URL**：見上方 Webhook URL（已存 `skills/credentials/line-bot.md`）
- **Channel ID**：`1654658337`
- **Channel Secret / Token**：Notion「MAPLAB API Keys」頁 + `bot/.env`

---

## 驗收標準

- [x] LINE OA inbound 訊息 → CONVERSATION_LOG 可看到同日資料（2026-05-19 已用 Sheet 只讀確認）
- [x] A6 Telegram 問「你現在是跑什麼模型」→ 由 A6 回 runtime/model 狀態，不啟動 A5
- [x] A6 一般聊天 route smoke → `codex exec` 實際回覆，非 Ollama 罐頭；fallback 才用 Ollama
- [ ] A6 Telegram `/linecases today` → 手機列出今日案件候選
- [ ] A6 Telegram `/case 客名` → 手機顯示案件摘要與近期 LINE 訊息
- [ ] A6 Telegram `/casequote 客名` → 手機收到 A5 本機備援報價草稿
- [ ] SALES_INTAKE 手動填一筆 → A6 輸出報價草稿
- [ ] 業務修改報價 → REVISION_LOG 有記錄
- [ ] **Verification（主觀任務）**：A6 一般聊天/`/case`/`/casequote` 案件摘要文字品質依 `rubrics/telegram-digest-quality.md` 評分（資訊密度/格式一致/措辭安全/資料層級透明），不是 agent 自己判斷

---

## 2026-07-07 23:47 CST — A6 Telegram takeover / route guard checkpoint

### Chrome Telegram 最近對話發現

- 22:16/22:17 Owner 問「我在這裡請你報價 有訓練到ollama 嗎...工作流拿給他用嗎？」時，A6 誤回 `A6 runtime 狀態`。
- 近期照片路徑曾回 `找不到 claude 命令`，這是舊 Claude CLI 裸錯，不是可接手的 Telegram UX。
- `15人有主食高毛利 要英文菜單` 的舊本地結果在 memory 中只剩本地備援 footer，手機上不可用。
- A6 chat window 沒有一鍵接手包，下一個 Codex 要靠人工拼上下文。

### 已修

- `bot_a6/bot_a6.py` 收斂 `_looks_like_runtime_status_request()`：只接受明確 `/status`、`/model`、短模型/status 問句；長句裡出現 `ollama` 不再觸發 status。
- 新增 `/takeover`、`/handoff`、`接手`、`🧭 接手包` keyboard，會產生 repo/runtime/冷啟動檔案/最近 local memory 的 copyable Codex 接手包。
- 非報價照片不再呼叫舊 Claude CLI；改回圖片保存路徑 + `/takeover` 接手方式。
- `/localquote`/本地 fallback 如果輸出空白、footer-only 或 JSON-heavy，改用 deterministic Sheet-first 摘要保底。
- 新增 `bot_a6/test_a6_telegram_routes.py`。

### 驗證

- `bot/venv/bin/python -m py_compile bot_a6/bot_a6.py bot_a6/a5_quote_engine.py bot_a6/test_a6_telegram_routes.py` passed。
- `bot/venv/bin/python bot_a6/test_a6_telegram_routes.py` passed。
- `launchctl kickstart -k gui/501/com.maplab.a6bot` 後 `launchctl list` 顯示 PID `29067` / status `0`。
- Chrome Telegram readback：
  - 23:38 `/takeover` → A6 回 `A6 接手包`，含 repo、launchd runtime、cold-start 檔案與最近 local memory。
  - 23:39 同一個誤判長句 → A6 回「A6 Codex 處理中…」後正常回答工作流/mini model 問題，沒有再回 runtime status。
  - 23:46 `/localquote 15人有主食高毛利 要英文菜單` → 23:47 回 `A5 Sheet-first 報價試算`，總金額 NT$15,700 / 成本 NT$3,140 / 毛利率 80%，並明確標示不寫 Google Sheet。

### Receipt

- `workbook/reviews/A6-TELEGRAM-TAKEOVER-20260707/validation_report.md`

---

## 接續 Prompt

你是 MAPLAB A6 業務快反應部隊，運行在 Mac mini Codex / Telegram bot runtime。
repo: `/Users/pagemacmini/maplab-ai-handbook`

先讀：
1. `CURRENT_STATUS.md`
2. `handoff/tasks/T-A6-001.md`
3. `projects/line-quote-assistant.md`
4. `decisions.md` 的 LINE / Telegram Bot / GAS 區塊
5. `pitfalls.md`

上次做到：Case Store v0 已新增 `bot_a6/case_store.py`，A6 bot 已新增 `/linecases`、`/case`、`/casequote`，並修正 A6 route guard：`/start` 預設一般聊天，`/status`/`/model`/模型問題由 A6 直接回覆，普通對話/SEO 先走 Codex read-only ephemeral，Codex 不可用才 fallback Ollama；只有明確報價才進 A5。不動 Google Sheet、不動 LINE webhook、不動 GAS 報價公式。

下一步：
1. 跑 `python3 -m py_compile bot_a6/case_store.py bot_a6/bot_a6.py`
2. 用同一個 bot runtime Python 跑 Codex smoke：`codex_ask(...)` 應實際回 Codex 答案；若 Codex CLI/額度失敗，確認 Telegram 會先發 fallback 通知再走 Ollama。
3. 用同一個 bot runtime Python 跑 route guard smoke：`你現在是跑什麼模型` 不得觸發 A5，`報價 王小明 婚禮 80人 預算6萬` 必須觸發 A5。
4. 跑 `python3 bot_a6/case_store.py today --rows 120 --limit 5`，確認 `source=`；若是 `fallback:` 代表本機 Google token 仍失效，不是 live Sheet。
5. 重啟/確認 `com.maplab.a6bot`
6. Telegram 實測一般聊天、`/status`、問模型、`報價 王小明 婚禮 80人 預算6萬`、`/linecases today`、`/case Penny`、`/casequote Penny`

Blocker：LINE inbound 同步可見，但完整雙向對話不在 webhook；如需完整訓練資料，改走 LINE OA Manager CSV 匯出。本機 `~/.claude/mcp-keys/google-token.json` 目前曾回 `invalid_grant`，若 A6 bot 要讀 live Sheet，需刷新 Google OAuth；刷新前 Case Store 會用 ignored runtime seed 檔做降級測試。

踩過的坑：`/Users/pagemacmini/Downloads/maplab-ai-handbook-main` 是非 git 舊副本；正式工作要在 `/Users/pagemacmini/maplab-ai-handbook`。`LineWebhook.gs` 被 `.claspignore` 排除是刻意保護，不要推到報價 GAS 專案。
