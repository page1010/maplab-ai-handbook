> 草稿未審(free-quota 班表 20260924,model=nvidia/nemotron-3-super-120b-a12b:free,item=AGENT_RULES.md#L241-300)。翻譯/生成稿:不得新增事實,上線前必經人審。

來源: AGENT_RULES.md#L241-300

| 條目標題 | 這一段在講什麼(30字內) | 關鍵字 | 對應行號範圍 |
|---|---|---|---|
| 強制存檔規則（Checkpoint Policy） | 所有 agent 必須以 commit 為存檔斷點，無例外。 | 強制存檔, commit, 必須 | 241-300 |
| 定時存檔頻率與 Checkpoint Commit 內容 | 定時存檔：30分鐘、任務階段、結束前皆需 commit；checkpoint commit 格式固定。 | 定時存檔, checkpoint, commit message, 必須 | 241-300 |
| 結束 Session 強制規則與 接續 Prompt | 離開前必須更新Task Card、寫接續Prompt、commit；接續 Prompt 必含模板。 | 結束session, 必須, TaskCard, 接續Prompt, Commit, 模板 | 241-300 |
| A1 Claude Code 額外規則 | A1 每次 commit 前必須檢查 Extension 變更及角色/任務狀態。 | A1, Claude Code, 必須檢查, Extension, CHANGELOG, AGENT_RECALL_PROMPTS | 241-300 |

可執行的規定:
每 30 分鐘 | 必須動作 | 至少 1 次 checkpoint commit（即使只是進度更新）
每次任務階段完成 | 更新 Task Card + commit
結束 session 前 | 必須寫接續 Prompt（見下方）
Agent 結束工作（關閉 tab、對話結束、即將斷線）前，**必須完成以下 3 件事**：
更新 Task Card — handoff/tasks/T-xxx.md 的「Done」「Next」「Blockers」區塊
寫接續 Prompt — Task Card 底部的「接續 Prompt」區塊，下一個接手的 agent 直接複製即可開工
Commit — 把以上修改 commit 到 GitHub
接續 Prompt 必須包含：
A1 每次 commit 前必須檢查：
