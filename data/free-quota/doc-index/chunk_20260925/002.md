> 草稿未審(free-quota 班表 20260925,model=nvidia/nemotron-3-super-120b-a12b:free,item=pitfalls.md#L781-840)。翻譯/生成稿:不得新增事實,上線前必經人審。

We need to produce an index entry (or multiple entries) for the segment. The segment includes multiple dated sections: 2026-08-28 — Plateau guard...; 2026-08-28 — Lexical evaluator...; 2026-08-28 — 收據pair自洽不等於live provenance...; 2026-08-28 — Plist bytes通過不等於launchd live route已切換; 2026-08-28 — 停止旗標若只靠last_result,checkpoint漏欄位會把pause變成permission; 2026-08-28 — Frozen case manifest不等於structured human labels，target也不能自動當全PASS gold; 2026-08-30 — Public leak validator不可把安全欄位名稱誤判成私密值; 2026-08-31 — Provider 行為改了，能力說明與 runtime state 必須同時改

We need to split into searchable index entries. Likely each dated bullet is an entry. Provide entry title, summary (30 chars Chinese), keywords (3-6), and line range (the source range). Since we cannot subdivide lines precisely, we can just give the whole source range for each entry (the entire segment). Or we could approximate line ranges per bullet but not required; instruction says "對應行號範圍(用來源那一行的範圍,無法細分就照抄)". So we must use the source line range (the one given in the source line). That is "pitfalls.md#L781-840". So each entry's line range is that same.

We need to output a table with four columns: 條目標題 / 這一段在講什麼(30 字內) / 關鍵字(3 到 6 個,逗號分隔) / 對應行號範圍.

We need to list each entry as a row in the table.

Then after table, a line "可執行的規定:" then list the sentences that contain 必須/不得/禁止/一律 from the segment, verbatim. If none, write "本段無".

We must not exceed 500 characters total. Need to be concise.

Let's extract the mandative sentences:

From each section:

Plateau guard section:
- "所有人工、heartbeat、launchd／cron入口都只能進同一 supervisor gate；"
- "plist source、installed copy與 launchctl live readback一起驗。"
- "Plateau狀態下kickstart必須 round／calls／attempt完全不變，並留下zero-call receipt；direct raw-loop route一律fail closed。"
- "每次新增 guard 時固定掃 canonical config、installed runtime copy、所有 scheduler與Resume Prompt；「code有guard」不等於「生產路徑受guard」。Guard修復前不得執行後續模型實驗。"
- "封坑驗證：method audit已證 repo＋installed plist同SHA但 `program_routes_direct_training_loop=true`、`program_routes_supervisor=false`，post-pause bypass receipt SHA `eb551990...`；next action已改為schedule gate，attempt維持6、audit model calls 0。" (this is not a mandate)

Lexical evaluator:
- "evaluator v1降級為診斷工具；先用固定20-case人工結構標籤把rubric v2校正到至少18/20，再凍結grader。"
- "E1只變 `prompt_builder_contract_sha256`，model／holdout／two-shot／seed／lesson snapshot／rubric／acceptance全固定，最多40 local calls；兩側完整rendered prompts與shared input manifest未pin前不得執行。"
- "預防：promotion gate必須先做adversarial calibration；任何single-variable實驗契約若列出兩個changed variables就拒絕執行。Development holdout不得回算七連勝，promotion另用預封存、互不重疊balanced panels。"
- "封坑驗證：..." not mandate.

收據pair自洽不等於live provenance:
- "解法：產收據前從canonical path重算job、model、supervisor、current lessons、training data與`build_prompt`來源；nested keys／types／timestamps exact驗證。完成後固定記錄 `preimage job SHA → private audit SHA → public receipt SHA/body → updated job SHA`，凍結四份artifact，不以更新後job重產舊audit。"
- "預防：任何method receipt升級control-plane前都跑live-source poison matrix；pair能互相對上但不能對上live preimage就fail closed。Control-plane更新後只驗hash chain與凍結bytes，下一輪才建立新的preimage。"
- "封坑驗證：..." not mandate.

Plist bytes通過不等於launchd live route已切換:
- "解法：file checker只可輸出`validated_plist_route`、`validated_plists_contain_raw_loop`、`installed_plist_verified`並固定`live_launchd_verified=false`。真正完成必須另驗tracked／installed exact SHA、`launchctl print` exact argv、post-bootstrap runs baseline、plain kickstart runs delta=1、new stdout reason、exit/active/PID及第二次穩定readback。"
- "預防：任何service config收據都拆`file truth`與`loaded runtime truth`；reload可能重置runs，delta只能從post-bootstrap baseline算，禁止把舊exit code或整份歷史log當本輪proof。"
- "封坑驗證：..." not mandate.

停止旗標若只靠last_result，checkpoint漏欄位會把pause變成permission:
- "解法：所有named `method-redesign-*` phase在沒有explicit `execution_eligible=true`時一律fail closed，且在receipt建立、dataset讀取與runner前返回零寫入。真正授權attempt>0 resume時，CLI root必須匹配canonical artifacts內唯一既有`line-training-supervisor-receipt`，missing／duplicate／wrong root皆拒絕。"
- "預防：每個pause gate固定有三種poison：explicit false＋missing receipt、named redesign＋missing latch、explicit true＋missing/wrong receipt binding；斷言runner=0、job bytes／attempt／receipt／run／lesson不變。"
- "封坑驗證：..." not mandate.

Frozen case manifest不等於structured human labels，target也不能自動當全PASS gold:
- "解法：先exact重建20案並做label-availability audit；缺人工labels就fail closed。Mixed specimen＋blank slots也不能直接進Owner gate，必須先凍結operational decision guide、overall公式、current commercial authority、named-human attestation/adjudication與每項criteria正反coverage。Human provenance與AI／synthetic prelabel分欄；scorer identity-blind，先獨立score內容再由harness比expected vector。"
- "預防：任何`*_ready=true`都要驗artifact schema、count、guide與coverage，不准只驗manifest count；Task Card必分case manifest、reply specimens、annotation guide／authority、human labels、scorer rules五個preimages。Historical target只能稱human-authored reference，未經具名真人逐項覆核不得稱human gold；blank packet不可原地改，annotations另檔綁parent SHA。"
- "封坑驗證：..." not mandate.

Public leak validator不可把安全欄位名稱誤判成私密值:
- "解法：public artifact仍拒絕absolute user path、private root、owner/chat identity與帳號資料，但允許在forbidden-input contract中明列安全欄位名稱；真正case hash／row hash值由artifact topology與fixture allowlist另行拒絕。"
- "預防：leak validator測試必同時含一個真私密值應REJECT與一個安全policy-name應PASS；安全掃描採key/value aware或明確allowlist，不以欄位名稱本身證明外洩。"
- "封坑驗證：..." not mandate.

Provider 行為改了，能力說明與 runtime state 必須同時改:
- No explicit mandate? The text ends: "根因：provider fallback policy 在 gateway 與 capability runtime 各自硬編，行為變更只改到執行路徑，沒有把能力投影納入同一驗收。" No mandate. Maybe earlier: "Provider 行為改了，能力說明與 runtime state 必須同時改" is a title but not a sentence with 必須/不得 etc. No explicit mandated sentence.

Thus we need to extract all sentences containing 必須/不得/禁止/一律.

Let's list them:

1. "所有人工、heartbeat、launchd／cron入口都只能進同一 supervisor gate；" (contains 只能, not mandatory word but maybe not required. We need only those with 必須/不得/禁止/一律. So skip.)

2. "plist source、installed copy與 launchctl live readback一起驗。" no.

3. "Plateau狀態下kickstart必須 round／calls／attempt完全不變，並留下zero-call receipt；direct raw-loop route一律fail closed。" Contains 必須 and 一律.

4. "每次新增 guard 時固定掃 canonical config、installed runtime copy、所有 scheduler與Resume Prompt；「code有guard」不等於「生產路徑受guard」。Guard修復前不得執行後續模型實驗。" Contains 不得.

5. "E1只變 `prompt_builder_contract_sha256`，model／holdout／two-shot／seed／lesson snapshot／rubric／acceptance全固定，最多40 local calls；兩側完整rendered prompts與shared input manifest未pin前不得執行。" Contains 不得.

6. "預防：promotion gate必須先做adversarial calibration；任何single-variable實驗契約若列出兩個changed variables就拒絕執行。Development holdout不得回算七連勝，promotion另用預封存、互不重疊balanced panels。" Contains 必須 and 不得.

7. "解法：產收據前從canonical path重算job、model、supervisor、current lessons、training data與`build_prompt`來源；nested keys／types／timestamps exact驗證。完成後固定記錄 `preimage job SHA → private audit SHA → public receipt SHA/body → updated job SHA`，凍結四份artifact，不以更新後job重產舊audit。" Contains 不得? Actually "不以更新後job重產舊audit" includes 不. Not exactly 不得 but contains 不. Not required? Only need 必須/不得/禁止/一律. "不以" is not exactly those words. Might not count. Safer to include only those with the exact words.

Check: "不以更新後job重產舊audit" is not one of the four. So skip.

8. "預防：任何method receipt升級control-plane前都跑live-source poison matrix；pair能互相對上但不能對上live preimage就fail closed。Control-plane更新後只驗hash chain與凍結bytes，下一輪才建立新的preimage。" Contains 不? "不能" not one of the four. So skip.

9. "解法：file checker只可輸出`validated_plist_route`、`validated_plists_contain_raw_loop`、`installed_plist_verified`並固定`live_launchd_verified=false`。真正完成必須另驗tracked／installed exact SHA、`launchctl print` exact argv、post-bootstrap runs baseline、plain kickstart runs delta=1、new stdout reason、exit/active/PID及第二次穩定readback。" Contains 必須.

10. "預防：任何service config收據都拆`file truth`與`loaded runtime truth`；reload可能重置runs，delta只能從post-bootstrap baseline算，禁止把舊exit code或整份歷史log當本輪proof。" Contains 禁止.

11. "解法：所有named `method-redesign-*` phase在沒有explicit `execution_eligible=true`時一律fail closed，且在receipt建立、dataset讀取與runner前返回零寫入。真正授權attempt>0 resume時，CLI root必須匹配canonical artifacts內唯一既有`line-training-supervisor-receipt`，missing／duplicate／wrong root皆拒絕。" Contains 一律 and 必須.

12. "預防：每個pause gate固定有三種poison：explicit false＋missing receipt、named redesign＋missing latch、explicit true＋missing/wrong receipt binding；斷言runner=0、job bytes／attempt／receipt／run／lesson不變。" No mandatory words.

13. "解法：先exact重建20案並做label-availability audit；缺人工labels就fail closed。Mixed specimen＋blank slots也不能直接進Owner gate，必須先凍結operational decision guide、overall公式、current commercial authority、named-human attestation/adjudication與每項criteria正反coverage。Human provenance與AI／synthetic prelabel分欄；scorer identity-blind，先獨立score內容再由harness比expected vector。" Contains 必須.

14. "預防：任何`*_ready=true`都要驗artifact schema、count、guide與coverage，不准只驗manifest count；Task Card必分case manifest、reply specimens、annotation guide／authority、human labels、scorer rules五個preimages。Historical target只能稱human-authored reference，未經具名真人逐項覆核不得稱human gold；blank packet不可原地改，annotations另檔綁parent SHA。" Contains 不得 and 不准? "不准" is not exactly one of four but similar to 不得? The instruction only counts 必須/不得/禁止/一律. So "不准" not included. "不得" is included. So we have "不得稱human gold". Also "不得" appears.

15. "解法：public artifact仍拒絕absolute user path、private root、owner/chat identity與帳號資料，但允許在forbidden-input contract中明列安全欄位名稱；真正case hash／row hash值由artifact topology與fixture allowlist另行拒絕。" No mandatory words.

16. "預防：leak validator測試必同時含一個真私密值應REJECT與一個安全policy-name應PASS；安全掃描採key/value aware或明確allowlist，不以欄位名稱本身證明外洩。" Contains 必須.

Now compile list of mandated sentences (verbatim). Need to ensure we copy exactly as in text, including punctuation.

Let's extract each:

- "Plateau狀態下kickstart必須 round／calls／attempt完全不變，並留下zero-call receipt；direct raw-loop route一律fail closed。"
- "Guard修復前不得執行後續模型實驗。"
- "兩側完整rendered prompts與shared input manifest未pin前不得執行。"
- "預防：promotion gate必須先做adversarial calibration；任何single-variable實驗契約若列出兩個changed variables就拒絕執行。Development holdout不得回算七連勝，promotion另用預封存、互不重疊balanced panels。"
- "真正完成必須另驗tracked／installed exact SHA、`launchctl print` exact argv、post-bootstrap runs baseline、plain kickstart runs delta=1、new stdout reason、exit/active/PID及第二次穩定readback。"
- "禁止把舊exit code或整份歷史log當本輪proof。" Wait the sentence: "禁止把舊exit code或整份歷史log當本輪proof。" The original: "禁止把舊exit code或整份歷史log當本輪proof。" Actually text: "禁止把舊exit code或整份歷史log當本輪proof。" Need to copy exactly: from segment: "禁止把舊exit code或整份歷史log當本輪proof。" Let's locate: In Plist bytes section: "預防：任何service config收據都拆`file truth`與`loaded runtime truth`；reload可能重置runs，delta只能從post-bootstrap baseline算，禁止把舊exit code或整份歷史log當本輪proof。" Yes.

- "所有named `method-redesign-*` phase在沒有explicit `execution_eligible=true`時一律fail closed，且在receipt建立、dataset讀取與runner前返回零寫入。" Wait that's part of solution sentence; but the mandated part is "一律fail closed". The sentence includes "一律fail closed". We need to capture the whole sentence? The instruction: list the sentences that contain 必須/不得/禁止/一律. So we should include the whole sentence that contains those words. So include the full sentence: "所有named `method-redesign-*` phase在沒有explicit `execution_eligible=true`時一律fail closed，且在receipt建立、dataset讀取與runner前返回零寫入。真正授權attempt>0 resume時，CLI root必須匹配canonical artifacts內唯一既有`line-training-supervisor-receipt`，missing／duplicate／wrong root皆拒絕。" Actually that's two sentences? The original text: "解法：所有named `method-redesign-*` phase在沒有explicit `execution_eligible=true`時一律fail closed，且在receipt建立、dataset讀取與runner前返回零寫入。真正授權attempt>0 resume時，CLI root必須匹配canonical artifacts內唯一既有`line-training-supervisor-receipt`，missing／duplicate／wrong root皆拒絕。" So there are two sentences separated by a period. The first contains 一律, second contains 必須. So we need to include both sentences.

Thus we list:

- "所有named `method-redesign-*` phase在沒有explicit `execution_eligible=true`時一律fail closed，且在receipt建立、dataset讀取與runner前返回零寫入。"
- "真正授權attempt>0 resume時，CLI root必須匹配canonical artifacts內唯一既有`line-training-supervisor-receipt`，missing／duplicate／wrong root皆拒絕。"

- "解法：先exact重建20案並做label-availability audit；缺人工labels就fail closed。Mixed specimen＋blank slots也不能直接進Owner gate，必須先凍結operational decision guide、overall公式、current commercial authority、named-human attestation/adjudication與每項criteria正反coverage。Human provenance與AI／synthetic prelabel分欄；scorer identity-blind，先獨立score內容再由harness比expected vector。" Contains 必須.

We need to capture the sentence containing 必須: that's "必須先凍結operational decision guide、overall公式、current commercial authority、named-human attestation/adjudication與每項criteria正反coverage。" That's part of a longer sentence. We'll include the whole sentence: "Mixed specimen＋blank slots也不能直接進Owner gate，必須先凍結operational decision guide、overall公式、current commercial authority、named-human attestation/adjudication與每項criteria正反coverage。Human provenance與AI／synthetic prelabel分欄；scorer identity-blind，先獨立score內容再由harness比expected vector。" Actually there are two sentences after the first part. The first sentence: "Mixed specimen＋blank slots也不能直接進Owner gate，必須先凍結operational decision guide、overall公式、current commercial authority、named-human attestation/adjudication與每項criteria正反coverage。" Second: "Human provenance與AI／synthetic prelabel分欄；scorer identity-blind，先獨立score內容再由harness比expected vector。" The first contains 必須. So we include that first sentence.

- "預防：任何`*_ready=true`都要驗artifact schema、count、guide與coverage，不准只驗manifest count；Task Card必分case manifest、reply specimens、annotation guide／authority、human labels、scorer rules五個preimages。Historical target只能稱human-authored reference，未經具名真人逐項覆核不得稱human gold；blank packet不可原地改，annotations另檔綁parent SHA。" Contains 不得. The sentence containing 不得: "Historical target只能稱human-authored reference，未經具名真人逐項覆核不得稱human gold；blank packet不可原地改，annotations另檔綁parent SHA。" That's one sentence (maybe two clauses). We'll include whole.

- "預防：leak validator測試必同時含一個真私密值應REJECT與一個安全policy-name應PASS；安全掃描採key/value aware或明確allowlist，不以欄位名稱本身證明外洩。" Contains 必須. The sentence: "租? Actually "預防：leak validator測試必同時含一個真私密值應REJECT與一個安全policy-name應PASS；安全掃描採key/value aware或明確allowlist，不以欄位名稱本身證明外洩。" That's one sentence (with a semicolon). We'll include whole.

Now also there is "禁止把舊exit code或整份歷史log當本輪proof。" Already captured.

Now need to output table rows for each dated section? The instruction: "把這一段拆成可搜尋的索引條目,讓其他人不必整份重讀就能找到需要的那幾行." Likely each dated bullet as entry. Provide entry title (maybe the date and short description). We'll need to produce maybe 8 entries.

Let's define entry titles as the date line (e.g., "2026-08-28 — Plateau guard 只包人工入口不夠；scheduler side-door 會繼續燒 calls").

Summary 30 chars Chinese: need to be within 30 characters. We'll craft concise.

Keywords 3-6 separated by commas.

Line range: same as source: pitfalls.md#L781-840.

Now produce table with columns: 條目標題 / 這一段在講什麼(30 字內) / 關鍵字 / 對應行號範圍.

We'll need to ensure total under 500 characters. Let's estimate.

We'll produce maybe 8 rows, each row short.

Let's craft each:

1. Title: "2026-08-28 — Plateau guard 只包人工入口不夠；scheduler side-door 會繼續燒 calls"
Summary: "守門必須統一入口，禁止直接raw-loop". Count characters: Let's count roughly: 守門必須統一入口，禁止直接raw-loop => maybe 20? OK.
Keywords: "Plateau guard,supervisor gate,kickstart,零呼叫" (4)
Line range: pitfalls.md#L781-840

2. Title: "2026-08-28 — Lexical evaluator 高分不等於業務正確，grader與prompt不能同輪一起改"
Summary: "評估需固定grader，禁止同時變prompt". (~20)
Keywords: "評估,grader,prompt,固定"
Line range same.

3. Title: "2026-08-28 — 收據pair自洽不等於live provenance，control-plane transition必須留下preimage chain"
Summary: "收據需可追溯，禁用僅pair相等". (~22)
Keywords: "收據,preimage,控制平面,不可偽造"
Line range.

4. Title: "2026-08-28 — Plist bytes通過不等於launchd live route已切換"
Summary: "plist檢查需驗證運行狀態，禁止僅靠檔案". (~22)
Keywords: "pl
