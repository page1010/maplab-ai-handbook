【Fable5 本人・同 session 續接】

兩件都動了，現況如實回報：

第二點（權限）：已改完。剛把 16 條指令白名單（我們自己的腳本、唯讀查詢、brew 安裝、pip 使用者安裝）和兩個資料夾權限寫進 Claude 設定檔，改前有留備份。下一輪續接生效，之後這類卡權限的事大幅減少。沒放行的：sudo、碰 secrets、動生產設定——這些照規矩仍然不開。

第三點（安裝）：兩條線這一刻都在跑——
一、拆解工具鏈（yt-dlp＋ffmpeg＋whisper-cpp）正在用 brew 裝。
二、富途 OpenD 正在從官方連結下載安裝（GUI 版，裝到應用程式資料夾，SDK 一併裝）。
裝完我寫進記事本，明早晨會或你下一則訊息時報結果。

富途有一步只有你能做：OpenD 裝好後要在它的視窗裡登入帳號（帳密只在 GUI 輸入，我全程不碰），登入後才能驗行情連線，一樣全程模擬盤。
