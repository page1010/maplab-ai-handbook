# T-A5-005: 報價狀態追蹤同步 + Dashboard

## 目標
1. onEdit trigger：報價單 N5（報價狀態）/ N7（匯款狀態）改變時，自動寫回 SALES_INTAKE 對應欄位
2. SALES_INTAKE 新增 L 欄（報價狀態）、M 欄（匯款狀態）
3. Dashboard 計數器：報價中 / 成交 / 未成交 / 已收訂金 / 已收全額 各幾筆

## 前置條件
- SALES_INTAKE 結構已確認（A-K 欄已有資料）✅
- 個別報價單 N5/N7 下拉驗證已存在（Code.gs v3.1）✅

## 架構設計

### onEdit Trigger 流程
1. 使用者在個別報價單修改 N5（報價狀態）或 N7（匯款狀態）
2. onEdit(e) 觸發 → 檢查修改位置是否為 N5 或 N7
3. 從該報價單讀取 case_id（固定位置，例如 N3）
4. 用 case_id 去 SALES_INTAKE 比對 A 欄 → 找到對應列
5. 寫入 L 欄（報價狀態）/ M 欄（匯款狀態）

### 技術限制
- onEdit simple trigger 不能跨 Spreadsheet → 必須用 installable trigger
- installable trigger 需要在每份新產出的報價單上安裝 → createQuote 裡要自動設定
- 或者改用定時 trigger 每小時掃一次所有報價單（較簡單但不即時）

### Dashboard
- 用 COUNTIF 公式：=COUNTIF(L:L,"報價中") 等
- 或在 SALES_INTAKE 最上方加摘要列

## 架構決策（已確定）
onEdit per-sheet installable trigger 太複雜（每份新報價單都要裝）。
改用**定時掃描**：每 30 分鐘跑 `syncQuoteStatus_()`，掃 SALES_INTAKE K 欄 URL → 讀 N5/N7 → 寫回 L/M。

## 子任務
- [x] SALES_INTAKE 加 L/M 欄標題（`ensureIntakeHeaders_()` 自動補）
- [x] 寫定時同步函數 `syncQuoteStatus_()`：掃 K 欄 → 開啟每份報價單 → 讀 N5/N7 → 寫回 L/M
- [x] `setupSyncTrigger()`：建立 time-driven trigger 每 30 分鐘執行
- [x] `setupDashboard()`：建立 Dashboard 分頁，COUNTIF 統計報價狀態/匯款狀態
- [x] `writeToIntake_()` 更新：L 欄初始值='報價中'，M 欄初始值='未匯'
- [ ] clasp push 到 GAS（下一步）
- [ ] 測試：手動改報價單 N5/N7，執行 syncQuoteStatus_()，確認 SALES_INTAKE L/M 更新
- [ ] GAS 手動執行 `setupSyncTrigger()` 一次（啟動定時）
- [ ] GAS 手動執行 `setupDashboard()` 一次（建立 Dashboard 分頁）

## 注意
- onEdit trigger 只在手動編輯時觸發，程式修改不會觸發
- 需要 installable trigger（不是 simple trigger），因為要跨 Spreadsheet 操作
- 規格文件: projects/quote-system-v2.md

## 狀態
🟡 進行中（程式碼已寫入 Code.gs，待 clasp push + 手動啟動 trigger）

## 負責
A5 / A0 可執行
