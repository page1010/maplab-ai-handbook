# SOP:看過才能說(先截圖辨識,不准腦補)

> 來源:Owner msg 6066(2026-09-23T22:35:43)
> 「你不要自己腦補 你至少桌面截圖 你又不截圖只看程式碼 sop 寫進去
> 遇到指令你覺得沒有先截圖辨識」
>
> 這份不是筆記,是**動工前的閘**。續接開場讀到 FABLE5_HANDOFF 頂部那行就必須走到這裡。

---

## 一、這條規矩要擋掉的具體失誤

2026-09-23 一個晚上同一個病犯了三次,全部發生在「畫面就在那裡,卻只讀程式與文字」:

| 輪次 | 講了什麼 | 憑什麼講的 | 實際 |
|---|---|---|---|
| 6059 | 「Chrome 裡沒有亞航」 | 只讀**最前面那個分頁** | 頁面在第 30 個分頁 |
| 6060 | 「那一頁從來沒在這台開過」 | AppleScript + CDP + 瀏覽紀錄 | **三條通道其實是同一條**,而且查錯設定檔 |
| 6065 | 「是結帳 session 逾時」 | 讀**網址參數**和**造訪時間**推的 | **沒看過畫面,沒讀過那行錯誤訊息** |

第三次最該記:結論可能是對的,但**取得方式是推論不是觀察**,而回覆沒有把這件事講清楚。
Owner 要的是「畫面上寫什麼」,拿到的是「從網址推測應該是什麼」。

**病根一句:能查到的東西查到就當成看到了。查到 ≠ 看到。**

---

## 二、閘:什麼情況下不准先講話

只要 Owner 的指令指向**一個看得到的東西**(某個網頁、某個畫面、某個視窗、某個 app、
某個後台、某張圖),而回答裡會出現下面任何一種句型,**動作順序一律是先取像、再開口**:

- 「找不到」「沒有」「不在這台」
- 「應該是…」「看起來像是…」「推測是…」
- 「原因是 X」——而 X 不是從畫面上讀到的字

### 取像的優先順序(由強到弱,能走上面就不走下面)

1. **截圖** — `bash scripts/a0_chrome_tab.sh shot [視窗 分頁]`
   帶視窗與分頁會先把該分頁切到前景再截。輸出路徑印在 stdout。
2. **讀整頁文字** — `bash scripts/a0_chrome_tab.sh owner-text <視窗> <分頁>`
3. **讀分頁標題** — `bash scripts/a0_chrome_tab.sh owner-title <視窗> <分頁>`
   (標題不需要「允許 Apple 事件的 JavaScript」,是 2 被擋時的退路)
4. **列出所有分頁** — `owner-list`(Owner 那份 Chrome)/ `list`、`cdp-list`(openclaw 那份)
5. **瀏覽紀錄** — `history-find <關鍵字>`(有沒有開過)、`history-when <關鍵字>`(放多久)

**1 到 3 是「看到」,4 到 5 只是「查到」。**
只有 4、5 而沒有 1~3 的時候,回覆裡**必須逐句標明哪一句是推論**,不得寫成結論句。

---

## 三、取像失敗怎麼辦(這條同樣重要)

取像失敗**不等於**可以回去用推論然後不講。失敗時回覆要同時給三樣:

1. **失敗原文**(例:`could not create image from display`)
2. **卡在哪一層**(權限 / 設定 / 程式 / 頁面本身)
3. **解除那一格的確切路徑**——一個動作、講得出在哪一個選單

已知的兩道鎖,都在 GUI 不在終端機:

| 要做的事 | 被什麼擋 | 解法(Owner 勾一個框) |
|---|---|---|
| 截圖 | 螢幕錄製權限 | 系統設定 > 隱私權與安全性 > 螢幕錄製 > 打開執行這個 session 的那個 app(目前是 Claude) |
| 讀整頁文字 | Chrome 的 AppleScript JS 開關 | Owner 那份 Chrome 選單列 > 檢視 > 開發人員 > 允許 Apple 事件的 JavaScript |

兩個都是唯讀用途:不點擊、不填欄位、不讀密碼欄的值、不切換帳號。

---

## 四、截圖本身的邊界(不可放寬)

- 截圖會拍到 Owner 整個桌面——信箱、後台、帳號頁、金鑰頁都可能入鏡。
- **一律存 `~/.maplab/screenshots/`,在 repo 外。絕不 commit、絕不進 Telegram、絕不送第三方。**
- 引用截圖只引用**讀到的那句話**,不貼路徑以外的內容。
- 需要長期留存的,寫成文字結論入檔,圖不入檔。

同一條延伸到網址:瀏覽紀錄與分頁網址會夾帶 accessToken / refreshToken / OAuth 授權碼
(2026-09-23 亞航實際踩到)。**任何印出網址的工具一律只留 scheme+host+path,
問號與井號後面砍掉。**

---

## 五、一句話版本(貼在腦袋前面那張)

> **畫面上的字,要用眼睛拿,不要用推理拿。**
> 拿不到就說拿不到、說卡在哪、說怎麼開,不要用推理補一個聽起來很順的答案。
