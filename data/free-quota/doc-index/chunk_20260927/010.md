> 草稿未審(free-quota 班表 20260927,model=nvidia/nemotron-3-super-120b-a12b:free,item=pitfalls.md#L781-840)。翻譯/生成稿:不得新增事實,上線前必經人審。

We need to produce index entries from the given internal document snippet. The snippet contains multiple sections each with date headings. We need to split into searchable index entries. Likely each heading (date heading) is a separate entry. Provide source line: "來源:" then the file line: "----- 檔案:pitfalls.md#L781-840 -----". Then a table with four columns: 條目標題 / 這一段在講什麼(30 字內) / 關鍵字(3 到 6 個,逗號分隔) / 對應行號範圍 (use source line range, cannot subdivide so just copy same range). Then after table, a line "可執行的規定:" then list sentences that contain "必須/不得/禁止/一律" from the segment, verbatim. If none, write "本段無".

We must not exceed 500 characters total. Must be Traditional Chinese.

We need to extract each date heading and its content until next heading. There are sections:

1) 2026-08-28 — Plateau guard 只包人工入口不夠；scheduler side-door 會繼續燒 calls

2) 2026-08-28 — Lexical evaluator 高分不等於業務正確，grader與prompt不能同輪一起改

3) 2026-08-28 — 收據pair自洽不等於live provenance，control-plane transition必須留下preimage chain

4) 2026-08-28 — Plist bytes通過不等於launchd live route已切換

5) 2026-08-28 — 停止旗標若只靠last_result，checkpoint漏欄位會把pause變成permission

6) 2026-08-28 — Frozen case manifest不等於structured human labels，target也不能自動當全PASS gold

7) 2026-08-30 — Public leak validator不可把安全欄位名稱誤判成私密值

8) 2026-08-31 — Provider 行為改了，能力說明與 runtime state 必須同時改

Thus 8 entries.

For each entry we need: 條目標題 (likely the heading title). 這一段在講什麼 (30 characters inside). Provide summary <=30 Chinese characters. Then 關鍵字 (3 to 6 keywords, comma-separated). Then 對應行號範囲 (we cannot subdivide, so use same as source: #L781-840). We'll copy exactly the source line after "來源:".

Then after table, "可執行的規定:" and list sentences that contain "必須/不得/禁止/一律". Need to extract those sentences from each section.

Let's go through each section and extract.

Section 1: Plateau guard ... Contains sentences with obligations:

- "所有人工、heartbeat、launchd／cron入口都只能進同一 supervisor gate；plist source、installed copy與 launchctl live readback一起驗。Plateau狀態下kickstart必須 round／calls／attempt完全不變，並留下zero-call receipt；direct raw-loop route一律fail closed."

Contains: "必須", "一律". Also maybe "必須" appears. Also "必須" and "一律". Also maybe "必須" appears earlier? Let's locate: "Plateau狀態下kickstart必須 round／calls／attempt完全不變" -> that's a "必須". Also "direct raw-loop route一律fail closed." -> "一律". Also "所有人工、heartbeat、launchd／cron入口都只能進同一 supervisor gate" uses "只能" not the required words. Also "必須" maybe elsewhere? Not needed.

We need to capture the sentences exactly as they appear in the text. The text includes line breaks. We'll need to extract sentences that contain the keywords. Probably easiest to copy the whole sentence(s) containing those words.

From section 1:

Sentence A: "所有人工、heartbeat、launchd／cron入口都只能進同一 supervisor gate；plist source、installed copy與 launchctl live readback一起驗。Plateau狀態下kickstart必須 round／calls／attempt完全不變，並留下zero-call receipt；direct raw-loop route一律fail closed."

That's a long sentence with semicolons. It contains "必須" and "一律". We'll include the whole sentence as is.

Also there is "預防：每次新增 guard 時固定掃 canonical config、installed runtime copy、所有 scheduler與Resume Prompt；「code有guard」不等於「生產路徑受guard」。Guard修復前不得執行後續模型實驗。" Contains "不得". So we need that sentence too.

Sentence B: "預防：每次新增 guard 時固定掃 canonical config、installed runtime copy、所有 scheduler與Resume Prompt；「code有guard」不等於「生產路徑受guard」。Guard修復前不得執行後續模型實驗。" Contains "不得". Also "不等於" not needed.

Thus we will list both sentences.

Section 2: Lexical evaluator ... Contains obligations:

- "解法：evaluator v1降級為診斷工具；先用固定20-case人工結構標籤把rubric v2校正到至少18/20，再凍結grader。E1只變 `prompt_builder_contract_sha256`，model／holdout／two-shot／seed／lesson snapshot／rubric／acceptance全固定，最多40 local calls；兩側完整rendered prompts與shared input manifest未pin前不得執行。" Contains "不得".

- "預防：promotion gate必須先做adversarial calibration；任何single-variable實驗契約若列出兩個changed variables就拒絕執行。Development holdout不得回算七連勝，promotion另用預封存、互不重疊balanced panels。" Contains "必須", "不得".

- "封坑驗證：v7 receipt凍結20 unique holdout、40 unique two-shot cases、77 prior eval IDs／68 prior conversations exclusion與prompt-builder-only fingerprint；baseline／candidate=`NOT_RENDERED`、shared input=`NOT_PINNED`、lesson snapshot=`NOT_MATERIALIZED`，execution仍因五項明列前置未完成而disabled。" No obligation words.

Thus we need sentences with those words.

From "解法：" sentence: "解法：evaluator v1降級為診斷工具；先用固定20-case人工結構標籤把rubric v2校正到至少18/20，再凍結grader。E1只變 `prompt_builder_contract_sha256`，model／holdout／two-shot／seed／lesson snapshot／rubric／acceptance全固定，最多40 local calls；兩側完整rendered prompts與shared input manifest未pin前不得執行。" Contains "不得". We'll include whole.

From "預防：" sentence: "預防：promotion gate必須先做adversarial calibration；任何single-variable實驗契約若列出兩個changed variables就拒絕執行。Development holdout不得回算七連勝，promotion另用預封存、互不重疊balanced panels。" Contains "必須" and "不得". We'll include whole.

Section 3: 收據pair自洽不等於live provenance，control-plane transition必須留下preimage chain

Obligations:

- "解法：產收據前從canonical path重算job、model、supervisor、current lessons、training data與`build_prompt`來源；nested keys／types／timestamps exact驗證。完成後固定記錄 `preimage job SHA → private audit SHA → public receipt SHA/body → updated job SHA`，凍結四份artifact，不以更新後job重產舊audit。" No obligation words? maybe "必須"? Not present.

- "預防：任何method receipt升級control-plane前都跑live-source poison matrix；pair能互相對上但不能對上live preimage就fail closed。Control-plane更新後只驗hash chain與凍結bytes，下一輪才建立新的preimage。" Contains "必須"? Not present. "不能" not needed.

Thus maybe no obligation sentences? Check for "必須/不得/禁止/一律". I don't see any. There's "必須"? Not present. There's "只能"? not needed. So maybe "本段無".

But check: "完成後固定記錄 ..." not obligation. So likely none.

Section 4: Plist bytes通過不等於launchd live route已切換

Obligations:

- "解法：file checker只可輸出`validated_plist_route`、`validated_plists_contain_raw_loop`、`installed_plist_verified`並固定`live_launchd_verified=false`。真正完成必須另驗tracked／installed exact SHA、`launchctl print` exact argv、post-bootstrap runs baseline、plain kickstart runs delta=1、new stdout reason、exit/active/PID及第二次穩定readback。" Contains "必須". Also "只可" not needed.

- "預防：任何service config收據都拆`file truth`與`loaded runtime truth`；reload可能重置runs，delta只能從post-bootstrap baseline算，禁止把舊exit code或整份歷史log當本輪proof。" Contains "禁止". Also "只能". So we have sentences with "必須" and "禁止".

Thus we need to extract those sentences.

Sentence A: "解法：file checker只可輸出`validated_plist_route`、`validated_plists_contain_raw_loop`、`installed_plist_verified`並固定`live_launchd_verified=false`。真正完成必須另驗tracked／installed exact SHA、`launchctl print` exact argv、post-bootstrap runs baseline、plain kickstart runs delta=1、new stdout reason、exit/active/PID及第二次穩定readback。" Contains "必須". We'll include whole.

Sentence B: "預防：任何service config收據都拆`file truth`與`loaded runtime truth`；reload可能重置runs，delta只能從post-bootstrap baseline算，禁止把舊exit code或整份歷史log當本輪proof。" Contains "禁止". We'll include whole.

Section 5: 停止旗標若只靠last_result，checkpoint漏欄位會把pause變成permission

Obligations:

- "解法：所有named `method-redesign-*` phase在沒有explicit `execution_eligible=true`時一律fail closed，且在receipt建立、dataset讀取與runner前返回零寫入。真正授權attempt>0 resume時，CLI root必須匹配canonical artifacts內唯一既有`line-training-supervisor-receipt`，missing／duplicate／wrong root皆拒絕。" Contains "必須". Also "一律". Also "必須".

- "預防：每個pause gate固定有三種poison：explicit false＋missing receipt、named redesign＋missing latch、explicit true＋missing/wrong receipt binding；斷言runner=0、job bytes／attempt／receipt／run／lesson不變。" No obligation words.

Thus we need sentence(s) with those words.

Sentence A: "解法：所有named `method-redesign-*` phase在沒有explicit `execution_eligible=true`時一律fail closed，且在receipt建立、dataset讀取與runner前返回零寫入。真正授權attempt>0 resume時，CLI root必須匹配canonical artifacts內唯一既有`line-training-supervisor-receipt`，missing／duplicate／wrong root皆拒絕。" Contains "一律" and "必須". We'll include whole.

Section 6: Frozen case manifest不等於structured human labels，target也不能自動當全PASS gold

Obligations:

- "解法：先exact重建20案並做label-availability audit；缺人工labels就fail closed。Mixed specimen＋blank slots也不能直接進Owner gate，必須先凍結operational decision guide、overall公式、current commercial authority、named-human attestation/adjudication與每項criteria正反coverage。Human provenance與AI／synthetic prelabel分欄；scorer identity-blind，先獨立score內容再由harness比expected vector。" Contains "必須". Also "不能" not needed.

- "預防：任何`*_ready=true`都要驗artifact schema、count、guide與coverage，不准只驗manifest count；Task Card必分case manifest、reply specimens、annotation guide／authority、human labels、scorer rules五個preimages。Historical target只能稱human-authored reference，未經具名真人逐項覆核不得稱human gold；blank packet不可原地改，annotations另檔綁parent SHA。" Contains "不得", "只能", "必須". Actually "Task Card必分..." includes "必". Also "不准". Not needed.

Thus we need sentences.

Sentence A (解法): "解法：先exact重建20案並做label-availability audit；缺人工labels就fail closed。Mixed specimen＋blank slots也不能直接進Owner gate，必須先凍結operational decision guide、overall公式、current commercial authority、named-human attestation/adjudication與每項criteria正反coverage。Human provenance與AI／synthetic prelabel分欄；scorer identity-blind，先獨立score內容再由harness比expected vector。" Contains "必須". We'll include whole.

Sentence B (預防): "預防：任何`*_ready=true`都要驗artifact schema、count、guide與coverage，不准只驗manifest count；Task Card必分case manifest、reply specimens、annotation guide／authority、human labels、scorer rules五個preimages。Historical target只能稱human-authored reference，未經具名真人逐項覆核不得稱human gold；blank packet不可原地改，annotations另檔綁parent SHA。" Contains "必須" (Task Card必分), "不得" (不得稱human gold), "只能" (只能稱). We need sentences containing the keywords. We'll include whole sentence.

Section 7: Public leak validator不可把安全欄位名稱誤判成私密值

Obligations:

- "解法：public artifact仍拒絕absolute user path、private root、owner/chat identity與帳號資料，但允許在forbidden-input contract中明列安全欄位名稱；真正case hash／row hash值由artifact topology與fixture allowlist另行拒絕。" No obligation words.

- "預防：leak validator測試必同時含一個真私密值應REJECT與一個安全policy-name應PASS；安全掃描採key/value aware或明確allowlist，不以欄位名稱本身證明外洩。" Contains "必須". Also maybe "必". So we need that sentence.

Sentence: "預防：leak validator測試必同時含一個真私密值應REJECT與一個安全policy-name應PASS；安全掃描採key/value aware或明確allowlist，不以欄位名稱本身證明外洩。" Contains "必須". We'll include whole.

Section 8: Provider 行為改了，能力說明與 runtime state 必須同時改

Obligations:

- The text ends abruptly: "行為變更只改到執行路徑，沒有把能力投影納入同一驗收。" No obligation words? Actually earlier: "Provider 行為改了，能力說明與 runtime state 必須同時改" is heading but not a sentence. The content: "觸發條件：A6 gateway 已依 Owner 指示停用本機 Ollama fallback，但 `/capabilities` 的獨立 runtime formatter 仍宣稱上游失敗後會使用 `gemma4:latest`。" No obligation. "根因：provider fallback policy 在 gateway 與 capability runtime 各自硬編，行為變更只改到執行路徑，沒有把能力投影納入同一驗收。" No obligation.

Thus maybe none. Check for "必須/不得/禁止/一律". None. So "本段無".

Now we need to format output:

First line: "來源: ----- 檔案:pitfalls.md#L781-840 -----"

Then a table with four columns: 條目標題 / 這一段在講什麼(30 字內) / 關鍵字(3 到 6 個,逗號分隔) / 對應行號範圍

We need to produce rows for each entry. The table likely markdown style? Not specified but we can do plain text with pipes.

We must keep within 500 characters total. That's tight. Let's estimate.

We have 8 rows. Each row includes title, summary (<=30 chars), keywords (3-6), and same line range.

We need to be concise.

Let's craft each entry.

First, source line: "來源: ----- 檔案:pitfalls.md#L781-840 -----"

Now table header maybe not required? It says "表格四欄:條目標題 / 這一段在講什麼(30 字內) / 關鍵字(3 到 6 個,逗號分隔) / 對應行號範圍". So we need to output rows with those columns separated by slash or maybe using spaces. We'll follow format: "條目標題 / 這一段在講什麼 / 關鍵字 / 對應行號範圍". Use slash as separator.

We'll need to keep each row short.

Let's decide titles: use the heading text (date and title). For brevity, maybe just the title after date? But we can include full heading.

We'll need to keep summary <=30 Chinese characters. Let's craft.

Entry1: Plateau guard ...

Title: "2026-08-28 — Plateau guard 只包人工入口不夠；scheduler side-door 會繼續燒 calls"

Summary: "Plateau狀態下需同一supervisor gate，禁止raw-loop側門". Count characters? Let's count: 平台? Actually Chinese characters: "Plateau狀態下需同一supervisor gate，禁止raw-loop側門". Let's count: P l a t e a u (maybe English counts as characters but okay). We'll trust <30.

Keywords: choose 3-6: "Plateau, supervisor, raw-loop, guard". That's 4.

Row: "2026-08-28 — Plateau guard 只包人工入口不夠；scheduler side-door 會繼續燒 calls / Plateau狀態下需同一supervisor gate，禁止raw-loop側門 / Plateau, supervisor, raw-loop, guard / #L781-840"

Entry2: Lexical evaluator ...

Title: "2026-08-28 — Lexical evaluator 高分不等於業務正確，grader與prompt不能同輪一起改"

Summary: "評估需固定grader，禁止同時改prompt". Count maybe okay.

Keywords: "評估, grader, prompt, 固定". 4.

Row: "2026-08-28 — Lexical evaluator 高分不等於業務正確，grader與prompt不能同輪一起改 / 評估需固定grader，禁止同時改prompt / 評估, grader, prompt, 固定 / #L781-840"

Entry3: 收據pair自洽不等於live provenance...

Title: "2026-08-28 — 收據pair自洽不等於live provenance，control-plane transition必須留下preimage chain"

Summary: "收據產生需追溯preimage鏈，禁止僅驗pair". Keywords: "收據, preimage, 控制平面, 鏈". 4.

Row: "2026-08-28 — 收據pair自洽不等於live provenance，control-plane transition必須留下preimage chain / 收據產生需追溯preimage鏈，禁止僅驗pair / 收據, preimage, 控制平面, 鏈 / #L781-840"

Entry4: Plist bytes通過不等於launchd live route已切換

Title: "2026-08-28 — Plist bytes通過不等於launchd live route已切換"

Summary: "Plist驗證需同時確認live launchd參數，禁止僅靠文件". Keywords: "Plist, launchd, 驗證, live". 4.

Row: "2026-08-28 — Plist bytes通過不等於launchd live route已切換 / Plist驗證需同時確認live launchd參數，禁止僅靠文件 / Plist, launchd, 驗證, live / #L781-840"

Entry5: 停止旗標若只靠last_result，checkpoint漏欄位會把pause變成permission

Title: "2026-08-28 — 停止旗標若只靠last_result，checkpoint漏欄位會把pause變成permission"

Summary: "暫停旗標需顯式execution_eligible，禁止依賴last_result". Keywords: "暫停, execution_eligible, last_result, 旗標". 4.

Row: "2026-08-28 — 停止旗標若只靠last_result，checkpoint漏欄位會把pause變成permission / 暫停旗標需顯式execution_eligible，禁止依賴last_result / 暫停, execution_eligible, last_result, 旗標 / #L781-840"

Entry6: Frozen case manifest不等於structured human labels，target也不能自動當全PASS gold

Title: "2026-08-28 — Frozen case manifest不等於structured human labels，target也不能自動當全PASS gold"

Summary: "凍結案例需人工標籤，禁止將未標註當PASS". Keywords: "凍結案, 人工標籤, PASS, 標註". 4.

Row: "2026-08-28 — Frozen case manifest不等於structured human labels，target也不能自動當全PASS gold / 凍結案例需人工標籤，禁止將未標註當PASS / 凍結案, 人工標籤, PASS, 標註 / #L781-840"

Entry7: Public leak validator不可把安全欄位名稱誤判成私密值

Title: "2026-08-30 — Public leak validator不可把安全欄位名稱誤判成私密值"

Summary: "安全欄位名稱允許出現，私密值必須被拒絕". Keywords: "安全欄位, 私密值, 漏洞檢測, 拒絕". 4.

Row: "2026-08-30 — Public leak validator不可把安全欄位名稱誤判成私密值 / 安全欄位名稱允許出現，私密值必須被拒絕 / 安全欄位, 私密值, 漏洞檢測, 拒絕 / #L781-840"

Entry8: Provider 行為改了，能力說明與 runtime state 必須同時改

Title: "2026-08-31 — Provider 行為改了，能力說明與 runtime state 必須同時改"

Summary: "提供者行為變更需同步更新能力說明與運行狀態". Keywords: "提供者, 能力說明, runtime, 同步". 4.

Row: "2026-08-31 — Provider 行為改了，能力說明與 runtime state 必須同時改 / 提供者行為變更需同步更新能力說明與運行狀態 / 提供者, 能力說明, runtime, 同步 / #L781-840"

Now after table, we need "可執行
