> 草稿未審(free-quota 班表 20260926,model=nvidia/nemotron-3-super-120b-a12b:free,item=AGENT_RULES.md#L1321-1380)。翻譯/生成稿:不得新增事實,上線前必經人審。

We need to output index entries from the given internal document excerpt. Provide source line with file and range as given: "----- 檔案:AGENT_RULES.md#L1321-1380". Then a table with columns: 條目標題 / 這一段在講什麼(30字內) / 關鍵字(3到6個,逗號分隔) / 對應行號範圍 (use same source range). Possibly multiple entries for distinct topics. Need to break the excerpt into searchable index entries. The excerpt includes multiple sections: AGENT_RULES.md content lines about restrictions, then "關聯", then SECTION 28 — 立標準權, then details, then SECTION 29 — 值班重複警報升級律. We should create entries for each major topic.

We must obey constraints: only content from the excerpt, no external info. Must output in Traditional Chinese. Must keep under 500 characters total. Must list "可執行的規定:" after table, listing sentences that contain 必須/不得/禁止/一律. We need to extract those sentences verbatim.

Let's locate those in the excerpt.

From the text:

- "- **不受本條放寬**：金鑰／token／cookie／帳密／授權碼、`secrets/` 與 `.env`、他人客戶資料、兒童照片與私有客照、夾帶權杖的網址與全桌面截圖。這四類與 hermes 的階段無關，永久禁止。" Contains "永久禁止". That's a prohibition phrase.

- "- **本條開放的是讀取與產出文字，不是階段升級。** S2 端到端未驗的事實不變，S3 仍不得開。" Contains "不得開". That's "不得".

- In SECTION 28 table: "照現有文件做事 | 全部角色、全部模型、全部免費鏈 | 找不到規定就回報卡住，**不得自己補一條**" Contains "不得自己補一條".

- Also maybe "新增治理檔的硬條件" bullet: "新增一份會被別人拿去照做的 `.md`，**必須**同一輪登錄進 `SYSTEM_DIRECTORY_INDEX.md`，否則視同沒建立——" Contains "必須".

- Also maybe "檢查方式：`bash scripts/doc_governance_check.sh`（漏登錄即 exit 1）。`selftest` 參數會故意塞一份不存在的檔名，抓不到就代表檢查器自己壞了。" No mandatory.

- "2026-09-24 第一次量測：最上層 24 份治理 `.md`（不含索引自己），**14 份沒登錄**，其中包含 `AGENT_CORE.md`、`SOP_SEE_BEFORE_YOU_SAY.md`、`NAMING_GLOSSARY.md` 這三份被點名必讀的。當輪補登錄完成，24/24。" Contains "必讀的" but that's not exactly 必須/不得/禁止/一律. It's "必讀" but we only list sentences with those specific words? The instruction: "只列這一段裡寫成「必須/不得/禁止/一律」的句子". So only those exact words. "必讀" not included.

- In SECTION 28: "只有最前沿模型那一席（A0／Fable5 線）" not.

- "非前沿席位的同類產出一律只能當**提案**，不得直接生效" Contains "一律" and "不得". Actually phrase: "非前沿席位的同類產出一律只能當**提案**，不得直接生效". So includes "一律" and "不得". Need to capture whole sentence? The sentence likely is that line: "非前沿席位的同類產出一律只能當**提案**，不得直接生效". We'll copy exactly as appears.

- Also "判斷用一句話測：**產出會不會變成別人下次的依據。**會 → 立標準。不會 → 照做。" No.

- Under "新增治理檔的硬條件": "新增一份會被別人拿去照做的 `.md`，**必須**同一輪登錄進 `SYSTEM_DIRECTORY_INDEX.md`，否則視同沒建立——". That's a sentence containing 必須.

- Also maybe "檢查方式：`bash scripts/doc_governance_check.sh`（漏登錄即 exit 1）。" No.

- In SECTION 29: "實證（2026-09-25 量測）：`claude-daily-operations/state/hermes_duty/` 全部 17 份值班報告（20260912 → 20260925-AM）**每一份**都含同一組 `BLOCKED_PREFLIGHT`" No.

- There might be other prohibitions: Not seeing.

Thus list of regulatory sentences:

1. "- **不受本條放寬**：金鑰／token／cookie／帳密／授權碼、`secrets/` 與 `.env`、他人客戶資料、兒童照片與私有客照、夾帶權杖的網址與全桌面截圖。這四類與 hermes 的階段無關，**永久禁止**。"

But note the phrase "永久禁止" appears within that sentence. We need to copy the sentence exactly as appears, not just the phrase. The sentence: "- **不受本條放寬**：金鑰／token／cookie／帳密／授權碼、`secrets/` 與 `.env`、他人客戶資料、兒童照片與私有客照、夾帶權杖的網址與全桌面截圖。這四類與 hermes 的階段無關，**永久禁止**。"

Check original text: "- **不受本條放寬**：金鑰／token／cookie／帳密／授權碼、`secrets/` 與 `.env`、他人客戶資料、兒童照片與私有客照、夾帶權杖的網址與全桌面截圖。這四類與 hermes 的階段無關，永久禁止。" Actually there is no bold around 永久禁止 in original? It shows "永久禁止。" not bold. In the excerpt: "**永久禁止**。" Wait let's re-check: The line: "- **不受本條放寬**：金鑰／token／cookie／帳密／授權碼、`secrets/` 與 `.env`、他人客戶資料、兒童照片與私有客照、夾帶權杖的網址與全桌面截圖。這四類與 hermes 的階段無關，永久禁止。" In the provided text: after the English phrase, there is a comma then "永久禁止。" No bold. Actually reading: "- **不受本條放寬**：金鑰／token／cookie／帳密／授權碼、`secrets/` 與 `.env`、他人客戶資料、兒童照片與私有客照、夾帶權杖的網址與全桌面截圖。這四類與 hermes 的階段無關，永久禁止。" Yes no bold. So we copy that sentence exactly.

2. "- **本條開放的是讀取與產出文字，不是階段升級。** S2 端到端未驗的事實不變，S3 仍不得開。" Contains "不得開". Copy entire sentence.

3. Table row: "找不到規定就回報卡住，**不得自己補一條**" but the full sentence? The line in table: "找不到規定就回報卡住，**不得自己補一條**". Likely the sentence is just that phrase. We'll copy as appears: "找不到規定就回報卡住，**不得自己補一條**". Might need to include surrounding context? We'll treat as sentence.

4. "新增一份會被別人拿去照做的 `.md`，**必須**同一輪登錄進 `SYSTEM_DIRECTORY_INDEX.md`，否則視同沒建立——". Copy that sentence.

5. "非前沿席位的同類產出一律只能當**提案**，不得直接生效". Copy that sentence.

Potential also: "永久禁止" already captured.

Any other? maybe "必讀的" not count.

Thus we have 5 regulatory sentences.

Now we need to create index entries. We need to decide number of entries. Could be one entry per major topic: e.g., "禁止項目與讀取規定", "照現有文件做事與立新標準", "新增治理檔的硬條件", "值班重複警報升級律". But we need to keep within 500 characters. Let's aim for maybe 4 entries.

We must provide table with four columns: 條目標題 / 這一段在講什麼(30字內) / 關鍵字(3到6個,逗號分隔) / 對應行號範圍.

Since we cannot subdivide line ranges, we can just use the source range for each entry: "AGENT_RULES.md#L1321-1380". Or we could try to approximate but instruction says: "對應行號範圍(用來源那一行的範圍,無法細分就照抄)". So we just copy same range for each entry.

Thus each entry will have same range.

Now need to keep total under 500 characters. Let's estimate.

First line: "來源:AGENT_RULES.md#L1321-1380" plus newline.

Then table header? Not specified but we need columns. Probably we need to output header row? The format says: "表格四欄:條目標題 / 這一段在講什麼(30字內) / 關鍵字(3到6個,逗號分隔) / 對應行號範圍". Likely we need to output rows with those columns separated by "/". Could include a header line like "條目標題 / 這一段在講什麼 / 關鍵字 / 對應行號範圍". Not explicitly required but seems implied. We'll include header for clarity.

Then each entry line.

After table, line: "可執行的規定:" then list sentences each on new line? Probably each sentence separated by newline.

We must keep under 500 characters. Let's craft concise entries.

We'll make 4 entries:

1. 條目標題: "禁止項目與讀取規定"
   段落講什麼: "列出不得處理的敏感資料與允許的文字讀取輸出"
   關鍵字: "禁止,敏感資料,讀取"
   行號範圍: same.

2. 條目標題: "照現有文件做事與立新標準"
   段落講什麼: "區分照做與新標準，規定誰可立標準及違規處理"
   關鍵字: "照做,立標準,最前沿模型"
   行號範圍: same.

3. 條目標題: "新增治理檔的硬條件"
   段落講什麼: "新增治理文件必須同輪登錄索引，否則視為未建立"
   關鍵字: "治理檔,登錄,必須"
   行號範圍: same.

4. 條目標題: "值班重複警報升級律"
   段落講什麼: "hermes值班助理重複警報，指出未認真上班浪費額度"
   關鍵字: "值班,警報,hermes"
   行號範圍: same.

Now we need to ensure each description is within 30 Chinese characters. Let's count.

Entry1 description: "列出不得處理的敏感資料與允許的文字讀取輸出" Count characters: 列(1)出2不3得4處5理6的7敏8感9資10料11與12允13許14的15文16字17讀18取19輸20出21。 That's 21 characters, OK.

Entry2: "區分照做與新標準，規定誰可立標準及違規處理" Count: 区1分2照3做4與5新6標7準8，規9定10誰11可12立13標14準15及16違17規18處19理20。 20 characters.

Entry3: "新增治理文件必須同輪登錄索引，否則視為未建立" Count: 新1增2治3理4文5件6必7須8同9輪10登11錄12索13引14，否15則16視17為18未19建20立21。 21 characters.

Entry4: "hermes值班助理重複警報，指出未認真上班浪費額度" Count: h e r m s? Actually Chinese characters: "hermes" counts as letters but okay. Let's count: h(1)e2r3m4e5s6值7班8助9理10重11複12警13報14，指15出16未17認18真19上20班21浪22費23額24度25。 25 characters.

All under 30.

Keywords: need 3 to 6, comma separated.

Entry1 keywords: "禁止,敏感資料,讀取" (3). OK.

Entry2: "照做,立標準,最前沿模型" (3). OK.

Entry3: "治理檔,登錄,必須" (3). OK.

Entry4: "值班,警報,hermes" (3). OK.

Now the table rows: format: "條目標題 / 這一段在講什麼 / 關鍵字 / 對應行號範圍". We'll include header line exactly: "條目標題 / 這一段在講什麼 / 關鍵字 / 對應行號範圍". Then each entry line.

After table, line: "可執行的規定:" then each regulatory sentence on its own line.

Now count characters to ensure <500.

Let's draft:

來源:AGENT_RULES.md#L1321-1380
條目標題 / 這一段在講什麼 / 關鍵字 / 對應行號範圍
禁止項目與讀取規定 / 列出不得處理的敏感資料與允許的文字讀取輸出 / 禁止,敏感資料,讀取 / AGENT_RULES.md#L1321-1380
照現有文件做事與立新標準 / 區分照做與新標準，規定誰可立標準及違規處理 / 照做,立標準,最前沿模型 / AGENT_RULES.md#L1321-1380
新增治理檔的硬條件 / 新增治理文件必須同輪登錄索引，否則視為未建立 / 治理檔,登錄,必須 / AGENT_RULES.md#L1321-1380
值班重複警報升級律 / hermes值班助理重複警報，指出未認真上班浪費額度 / 值班,警報,hermes / AGENT_RULES.md#L1321-1380
可執行的規定:
- **不受本條放寬**：金鑰／token／cookie／帳密／授權碼、`secrets/` 與 `.env`、他人客戶資料、兒童照片與私有客照、夾帶權杖的網址與全桌面截圖。這四類與 hermes 的階段無關，永久禁止。
- **本條開放的是讀取與產出文字，不是階段升級。** S2 端到端未驗的事實不變，S3 仍不得開。
- 找不到規定就回報卡住，**不得自己補一條**
- 新增一份會被別人拿去照做的 `.md`，**必須**同一輪登錄進 `SYSTEM_DIRECTORY_INDEX.md`，否則視同沒建立——
- 非前沿席位的同類產出一律只能當**提案**，不得直接生效

Now count approximate characters.

First line: "來源:AGENT_RULES.md#L1321-1380" length about 30.

Header line: "條目標題 / 這一段在講什麼 / 關鍵字 / 對應行號範圍" length maybe ~45.

Each entry line: Let's approximate ~80 each? 4 entries => 320.

Then "可執行的規定:" line ~10.

Then each regulation sentence lines maybe ~80 each *5 =400.

Total maybe >500. Need to reduce.

We need to keep total under 500 characters. That's tight. We need to be very concise.

We can reduce number of entries maybe to 2 or 3. Also we can shorten regulation sentences? Must copy verbatim, can't shorten. But we can maybe combine some sentences? No, must list each sentence exactly; we can't omit.

But we could reduce entries to 2, and maybe combine some regulation sentences? But we need to list all sentences that contain the keywords. We could maybe note that some sentences are similar? But we must list each sentence exactly as appears; we cannot omit any. So we must include all 5 sentences. That will add significant characters.

We need to see if total can still be under 500 with minimal overhead.

Let's compute approximate characters more precisely.

We'll write in plain text without extra spaces maybe.

First line: "來源:AGENT_RULES.md#L1321-1380" (let's count) 
- "來源:" 3? Actually Chinese characters count 2? Let's just approximate.

I'll count using rough but we need to be safe.

Better approach: Keep entries minimal: maybe just one entry covering all? But we need to reflect different topics; could combine into one entry with a broader description. The requirement: "把這一段拆成可搜尋的索引條目,讓其他人不必整份重讀就能找到需要的那幾行." It doesn't mandate multiple entries; we could create a single entry summarizing the whole segment. Then we list all regulation sentences under that entry. That would reduce overhead.

Thus we can have 1 entry.

Entry: 條目標題: "AGENT_RULES.md片段索引"
這一段在講什麼: "規範敏感資料處理、讀取文字、照做與立標準、治理檔登錄及值班警報"
Need <=30 chars.

Let's craft: "敏感資料、讀取、立標準、治理檔、值班警報" Count: 敏1感2資3料4、5讀6取7、8立9標10準11、12治13理14檔15、16值17班18警19報20. That's 20 characters.

Keywords: need 3-6: maybe "敏感資料,讀取,立標準". That's 3.

Now table: one row.

Then regulation sentences list same.

Now compute total characters.

Let's draft final output:

來源:AGENT_RULES.md#L1321-1380
條目標題 / 這一段在講什麼 / 關鍵字 / 對應行號範圍
AGENT_RULES.md片段索引 / 敏感資料、讀取、立標準、治理檔、值班警報 / 敏感資料,讀取,立標準 / AGENT_RULES.md#L1321-1380
可執行的規定:
- **不受本條放寬**：金鑰／token／cookie／帳密／授權碼、`secrets/` 與 `.env`、他人客戶資料、兒童照片與私有客照、夾帶權杖的網址與全桌面截圖。這四類與 hermes 的階段無關，永久禁止。
- **本條開放的是讀取與產出文字，不是階段升級。** S2 端到端未驗的事實不變，S3 仍不得開。
- 找不到規定就回報卡住，**不得自己補一條**
- 新增一份會被別人拿去照做的 `.md`，**必須**同一輪登錄進 `SYSTEM_DIRECTORY_INDEX.md`，否則視同沒建立——
- 非前沿席位的同類產出一律只能當**提案**，不得直接生效

Now let's count roughly.

First line: "來源:AGENT_RULES.md#L1321-1380" length: let's count characters including letters and symbols.

"來源:" 3? Actually "來源" two Chinese characters plus colon = maybe 3 bytes but char count 2? We'll approximate 3.

"AGENT_RULES.md#L1321-1380": length: "AGENT_RULES.md" (13) + "#" (1) =14 + "L1321-1380" (9) =23. So total maybe ~26.

So first line ~29.

Second line header: "條目標題 / 這一段在講什麼 / 關鍵字 / 對應行號範圍"
Count: 條(1)項2標3目4 空5/6空7這8一9段10在11講12什13麼14 空15/16空17關18鍵19字20 空21/22空23對24應25行26號27範28圍29. plus spaces and slashes. Let's approximate 35.

Third line entry: "AGENT_RULES.md片段索引 / 敏感資料、讀取、立標準、治理檔、值班警報 / 敏感資料,讀取,立標準 / AGENT_RULES.md#L1321-1380"

Count roughly: "AGENT_RULES.md片段索引" length: "AGENT_RULES.md" 13 + "片段"2 + "索引"2 =17? Actually "片段" 2, "索引"2 => total 13+2+2=17. Then space slash space etc.

We'll approximate line length ~80.

Then "可執行的規定:" line length maybe ~12.

Now each regulation sentence lines: let's approximate each ~100 characters? 5*100=500. That would exceed.

We need to see actual length.

Let's compute each sentence length precisely.

Sentence1: "- **不受本條放寬**：金鑰／token／cookie／帳密／授權碼、`secrets/` 與 `.env`、他人客戶資料、兒童照片與私有客照、夾帶權杖的網址與全桌面截圖。這四類與 hermes 的階段無關，永久禁止。"

Count characters (including spaces). Let's approximate: It's long maybe ~120.

Sentence2: "- **本條開放的是讀取與產出文字，不是階段升級。** S2 端到端未驗的事實不變，S3 仍不得開。" maybe ~80.

Sentence3: "- 找不到規定就回報卡住，**不得自己補一條**" maybe ~40.

Sentence4: "- 新增一份會被別人拿去照做的 `.md`，**必須**同一輪登錄進 `SYSTEM_DIRECTORY_INDEX.md`，否則視同
