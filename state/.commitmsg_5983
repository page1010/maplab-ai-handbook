docs(spread): 永豐大小台互抵建檔骨架 — 回 5983(圖未進系統,已查出掉件證據)

Owner msg 5983:「根據我傳的圖 建檔--- 永豐封全球手機 大小台互抵介面，
到時候你要寫出一個介面，你先偵測到掛一筆價差通知我--讓我互抵釋放保證金，
你迅速再去檢查並掛收斂價差的單」

⚠ 本輪由 Opus 接手(bot.log 16:55:24 A0 primary model quota exhausted)，
  回覆已標【Opus 接手】。

== 🔴 圖未進系統,不是 Owner 沒傳,是我方管線掉件(任務 #56) ==
  1. data/telegram-photos/ 今日零張,最後一張 2026-09-22 22:20 (msg 5907)
  2. a0_inbox.jsonl 今日零筆 [📷 圖片](全檔共 32 筆)
  3. **訊息編號 5982 整段不存在**(inbox 只有 5981 與 5983)-> 圖確實發出
  同分鐘 bot.log 有 quota exhausted -> opus fallback,
  **是否同根因未經驗證,檔內與回覆皆標「推測」,不得當結論引用。**
  已請 Owner 重傳。

== 建檔(不等圖,先放已查證事實) ==
data/spread-offset/SINOPAC_OFFSET_UI_SPEC.md
  1 已查證事實表(含來源層級與查證日):
    互抵比例 微:小=5:1 / 微:大=20:1 / 小:大=4:1;申請時段 08:45-14:00;
    互抵免費免稅;MXF 保證金 175,250 / TMF 35,050(taifex, 9/23);
    **期交所組合減收清單=期貨配選擇權,期貨對期貨反向不在清單**(9/21);
    價差實測 9/21 盤中 3.4->2.4 點,門檻 10 點,引擎 9/17 起零觸發。
  1.1 機制澄清:**部位互抵=沖銷平倉**,保證金回來是因為部位沒了 ->
      互抵同時就是「出場」,而且零成本(免費免稅)。
      對照 ORB 一般出場要付 12 點 -> 出場零成本才是本策略的結構性價值。
      正確循環=鎖一組 -> 等收斂 -> 互抵零成本出場 -> 保證金回籠 -> 再鎖下一組。
  2 資金容量:一組=MXF1(175,250)+TMF5(175,250)=350,500;
      資本 100 萬 -> 最多同時 2 組。兩腳同持期間全額雙邊收,無折扣。
  3 介面三段狀態機:A 偵測(含報價品質閘防幽靈價差)/
      B 六欄推播缺一不發(含**互抵窗口剩餘分鐘**,14:00 關窗)/
      C 收斂核對(兩腳歸零+保證金回籠對帳,對不上停手回報、不自行推論)。
  4 紅線:實盤不代下單;互抵鈕只有 Owner 能按;模擬可全自動;
      實盤需開 Owner 9/20 已裁決的 A 案閘(事前授權+口數/日上限)——**至今未開**。
  5 待補(圖到再補):選單層級/欄位必填/口數是否 App 自動換算/確認頁/
      成功失敗文案/可否自助/單次上限。
  6 兩個未核參數(任務 #57,須 Owner 致電永豐):
      ①互抵計價方式(兩腳是否各按自家結算價)——決定鎖住的價差出場時還在不在,
        9/20 即標「全案最關鍵未核參數」,至今未核,已對 Owner 認。
      ②申請通道 App自助/網頁/僅營業員——決定推播後反應時間,決定門檻寬度。
  7 誠實提醒:此線目前是冷的,做它的理由是出場零成本,不是頻率。

未做不宣稱:介面尚未寫一行程式,本輪只有建檔。

Co-Authored-By: Claude <noreply@anthropic.com>
