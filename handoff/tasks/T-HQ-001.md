# T-HQ-001 — AGENT-HQ 集團共用層遷移

## 接續狀態
- **狀態**: 🔄 進行中
- **最後活動**: 2026-06-11
- **接續點**: 架構文件已落檔（docs/agent-hq-architecture.md），下一步 Phase 1 建 repo
- **阻塞**: 無
- **assigned_session**: 2026-06-11 / B1
- **last_committed_by**: B1（本 commit）

建立：2026-06-11
負責：B1
依據：Owner 指示「你來解決這個問題……想一個架構不用再有這種共用檔案但會找不到的問題」

## 目標

共用資產（chrome-extension / governance / panel / runtime / shared skills）遷出 maplab-ai-handbook，
建立 `agent-hq` 集團平台 repo，三個固定絕對路徑根目錄，永久解決跨專案找不到檔案的問題。

完整設計：`docs/agent-hq-architecture.md`

## Phases

- [ ] P1: 建 `page1010/agent-hq` private repo + clone 到 `/Users/pagemacmini/agent-hq`
- [ ] P2: 遷 chrome-extension（git mv → symlink 改指 → Chrome 三層驗證）
- [ ] P3: 遷 governance / panel / shared skills（maplab 與 ios 留指向檔）
- [ ] P4: launchd plists 登記 runtime/ + Owner 手動改 `~/.claude/settings.json` additionalDirectories
- [ ] P5: data-policy 落地（log rotate / archive 壓縮 / Owner 改 Drive 串流）
- [ ] P6: Hermes 記憶啟用 + A7 LINE JSONL export 訓練管線

## Owner 待辦（只有你能做的兩件事）

1. Google Drive 桌面設定 → 改「串流檔案」（釋放最多 531GB）
2. `~/.claude/settings.json` 加 additionalDirectories（auto mode 不讓 agent 改全域 settings）

## Guardrails

- 每 Phase 一個 commit，可 git 還原
- chrome-extension 遷移必過三層驗證（source 改 / Extensions 頁 active / popup 實際畫面）
- 不碰 secrets / .env
- 不合併兩家公司 repo
