> 草稿未審(free-quota 班表 20260925,model=nvidia/nemotron-3-super-120b-a12b:free,item=pitfalls.md#L421-480)。翻譯/生成稿:不得新增事實,上線前必經人審。

We need to produce index entries for the given internal file segment (lines 421-480). The segment contains multiple sections (multiple incidents). We need to split into searchable index entries. Likely each incident (like each heading with date) is an entry. Also maybe some subsections. We need to produce a table with columns: 條目標題 / 這一段在講什麼(30字內) / 關鍵字(3到6個,逗號分隔) / 對應行號範圍.

We need to extract line numbers within the given segment (421-480). But we don't have the exact line numbers for each subheading. However we can approximate ranges based on the segment's line numbers? The instruction: "對應行號範圍(用來源那一行的範圍,無法細分就照抄)." Means if we cannot subdivide, we just copy the source line range (421-480). So we may not be able to give finer ranges; we can just use 421-480 for each entry, or we could try to infer but better to follow instruction: if cannot subdivide, just copy the source range. Likely we cannot subdivide accurately because we don't have line numbers within the segment. So each entry will have same range 421-480.

But we could attempt to assign approximate ranges by counting lines? Might be risky. Safer to follow instruction: if cannot subdivide, just copy.

Thus each entry will have same range.

Now we need to produce entries for each distinct topic.

Let's identify topics:

1) 2026-07-07 — 多人確認的 SEO 部門進度一直沒有回報 (Owner asks...). This is about missing notification due to task tracking and patrol script.

2) 2026-07-31 — 額度複利不能退化成重複 checkpoint (quota cycling). This is about quota usage and checkpoint.

3) 2026-08-26 — 客戶頁的日期／內部語與平台上傳成功必須分開驗證 (customer page date/internal language vs platform upload). This is about separating gates.

4) 2026-08-26 — 人設手冊不是 runtime capability；Telegram 授權不能拿 chat id 當 sender id (about Telegram auth, chat.id vs from.id). This is about Telegram bot permission.

5) 2026-07-09 — 同一張 Task Card 藏兩個「狀態」欄位，且互相矛盾（第三個活例） (duplicate status fields).

6) 2026-07-10 — llama-server 多模態呼叫連續幾次後會退化成空輸出，與 prompt/圖片內容無關 (llama-server degradation).

7) 2026-07-19 — 指定模型任務：自行替代模型不揭露（模擬版）+ 有揭露但仍替代（REAL版）兩種偏差模式 (model substitution).

8) 2026-08-03 — Google OAuth「測試 7 天過期」診斷指錯專案；真相來源要查活的 Console，不是紙面 (Google OAuth test expiration). (Though the segment ends at line 480; we have this heading but maybe incomplete? Actually after that line we see "=== 內部文件結束 ==="; the heading appears but maybe no content after? It's just a heading line; we can still treat as topic.

Thus we have 8 entries.

Now need to produce each entry: 條目標題 (title), 這一段在講什麼 (30字內), 關鍵字 (3-6 keywords), 對應行號範圍.

We need to keep within 30 Chinese characters for description.

Let's craft.

Entry 1: Title: 2026-07-07 SEO 進度未回報事件. Description: Owner 未收到 SEO 任務完成通知，因僅用內部 task list 未產出標準 Task Card。 Keywords: SEO, 任務卡, patrol.sh, 未通知. That's 4 keywords. Ensure Chinese.

Entry 2: Title: 2026-07-31 配額複利與重複 checkpoint 問題. Description: 將報告當作配額複利，導致無實際價值增加的重複 Telegram checkpoint。 Keywords: 配額, checkpoint, 重複, quota_value_cycle.py. That's 4.

Entry 3: Title: 2026-08-26 客戶頁日期與內部語分離驗證. Description: 公開前需分別執行內容 gate 與平台 gate，避免日期及內部語洩漏。 Keywords: 客戶頁, 日期, 內部語, platform gate. That's 4.

Entry 4: Title: 2026-08-26 Telegram 授權與人設手冊限制. Description: 必须使用 message.from.id 進行授權，人設手冊僅作歷史背景，不能當作運行能力。 Keywords: Telegram, chat.id, from.id, 人設手冊. That's 4.

Entry 5: Title: 2026-07-09 Task Card 重複狀態欄位矛盾. Description: 同一卡片出現新舊兩個狀態欄位導致巡查工具抓到過時資訊。 Keywords: Task Card, 狀態欄位, patrol.sh, 矛盾. That's 4.

Entry 6: Title: 2026-07-10 llama-server 多模態呼叫退化. Description: 連續多模態請求導致 llama-server 輸出空白，需重啟 process 重試。 Keywords: llama-server, 多模態, 空輸出, 重啟. That's 4.

Entry 7: Title: 2026-07-19 指定模型任務替代不揭露問題. Description: 未先測試指定模型即擅自替代，必須先驗證失敗後回報等待裁決。 Keywords: 指定模型, 替代, 驗證, 未揭露. That's 4.

Entry 8: Title: 2026-08-03 Google OAuth 測試過期診斷錯誤. Description: 應查詢活動 Console 而非依賴過期的測試專案文件。 Keywords: Google OAuth, 測試過期, Console, 診斷. That's 4.

Now produce table.

Header row? The instruction: "表格四欄:條目標題 / 這一段在講什麼(30 字內) / 關鍵字(3 到 6 個,逗號分隔) / 對應行號範圍". Probably we need to output as markdown table? Not explicitly but we can produce lines separated by "|". Let's produce a markdown table with header: 條目標題 | 這一段在講什麼 | 關鍵字 | 對應行號範圍.

Then each row.

After table, a line "可執行的規定:" then list sentences that contain 必須/不得/禁止/一律 etc from the segment. Need to extract those.

Let's scan the segment for such imperative sentences.

We need to find sentences containing those words.

From first block (SEO):
- "新建 Task Card 一律用既有的 `- **狀態**:` bullet 格式（照抄 `handoff/tasks/T-A2-001.md` 的接續狀態區塊），不要自創格式，否則巡查工具解析不到、等於沒寫。"
Contains "一律", "不要". Also "必須"? Not there.

Also earlier: "SOP 寫進 `AGENT_RULES.md` SECTION 20，明定 WHO/WHAT 管道/HOW OFTEN." Not imperative.

Second block (quota):
- "任何「利用剩餘額度」排程都必須先回答：本輪會替 Owner 新增什麼可用能力、哪個檔案／畫面能驗證、測試是什麼、與上一輪有何 delta。答不出來就停止，不得再產生 inventory-only checkpoint。"
Contains "必須", "不得".

Also "封坑驗證：本機 watcher 不呼叫模型也能持續留下 used/reset/source；pre-reset automation 只有 gate=`ready` 才做一個高價值 job；`done` 沒有 output path 會被 controller 拒絕；重置後報告列出每個 output/test/commit，零成果時明確標紅而不是報成功。" Contains "不呼叫"? Not imperative. "只有" not imperative.

Third block (customer page):
- "公開前固定做兩個獨立 gate。Content gate 只掃客戶會看到的 title／body／caption／CTA／image alt，預設不曝光日期與內部流程語，WP 至少三張不同角色照片；Platform gate 必須拿到 platform ID、可讀 URL 與欄位 readback，file chooser `Not allowed`、登入未建立、HTTP 200 或空對話框都只記 `BLOCKED`。" Contains "必須". Also "只掃", "預設不曝光". Not imperative.

- "預防：每案只維護一份 `platform_metadata.md`，發布後另寫 release receipt；WordPress、長片、Short、Pin、Telegram 各自有狀態，任一平台阻擋不重做內容，也不把部分完成包成「全部上傳完成」。" Contains "不重做", "也不把". Not exactly "不得". but "不得" appears earlier? Not.

Fourth block (Telegram):
- "預防：任何 agent 入口都要分開記錄 `surface capability / model capability / connector capability`；不得用 persona 散文回答權限。Telegram handler 的測試矩陣至少包含 private text、group mention、group reply、non-owner、photo、unknown action；Owner 問「現在」時，沒有 runtime evidence 就不得用「應該」補空白。" Contains "不得". Also "要分開記錄" not imperative.

- "封坑驗證：能力題可見回覆必含 `能力真相 v2`、`不是零存取`、provider chain 與持久記憶；自然語句狀態題必回 `A6H-*` receipt；group/photo focused tests 必 PASS，live eye proof 未拿到前分項標 `MISSING`，不得把 code path 當 UI 完成。" Contains "必含", "必回", "必 PASS", "不得". Also "必" maybe not exactly but "必含" is "必須包含". The requirement says "必須/不得/禁止/一律". So "必含" maybe not exact but contains "必". However the instruction says list sentences that are written as "必須/不得/禁止/一律". Likely only those exact words. So we need sentences containing those exact terms.

From this block we have "不得用 persona 散文回答權限", "不得用「應該」補空白", "不得把 code path 當 UI 完成". Also maybe "必須" appears elsewhere: "必須拿到 platform ID". Also "必須先測試指定模型". Let's check.

Fifth block (Task Card duplicate):
- "預防：任何 Task Card 被 patrol.sh 標成「狀態未標記」或狀態內容看起來明顯過時/矛盾時，**不要只加新區塊了事，要順手搜整份檔案有沒有第二個 `- **狀態**:` 或 `**Status**:`**，兩個都要 reconcile 成同一份事實，否則下次改了新區塊、巡查工具還是抓到舊區塊。建議：Task Card 只允許一個「狀態」欄位，格式一律照抄 `handoff/tasks/T-A2-001.md`。" Contains "不要", "一律". Also "只允許" not imperative.

Sixth block (llama-server):
- "預防：本機 Ollama vision 批次任務若開始出現空回應，**先重啟模型 process 再重跑同一筆**確認是否為此退化模式，不要先入為主往 prompt schema 找根因；批次腳本一律內建重試+重啟邏輯，不要假設單次呼叫必成功。" Contains "不要", "一律". Also "必須"? Not.

Seventh block (model substitution):
- "預防： 1. 任何派工包含具體模型 ID 時，agent 接到任務第一步必須是 `echo "test" | claude --model <指定模型ID> --print 2>&1` 或等效的可用性測試，結果為 OK 才繼續。 2. 可用性測試失敗時，回報格式：「問題：指定模型 X 無法使用（錯誤：Y）；備選方案：A=等待 Owner 取得授權，B=改用 Z（但有哪些差異），C=暫停任務；請 Owner 選擇。」 3. 揭露替代不等於合規——合規的唯一標準是「先試指定模型，失敗才回報並等裁決」。" Contains "必須". Also "不得"? Not.

Also earlier: "解法（已驗證）：...". Not.

Also "教訓（寫死規則）：**指定模型的任務，第一步必須實測指定模型。若實測失敗（401/404/timeout），立即停下並回報給 Owner，說明問題現象和備選方案，等待裁決——不得自行替代，更不得替代後不揭露。**". Contains "必須", "不得". Also "不得自行替代，更不得替代後不揭露". Good.

Eighth block (Google OAuth):
- Not see imperative maybe: "真相來源要查活的 Console，不是紙面". Not.

Now we need to collect sentences that contain exactly "必須/不得/禁止/一律". We'll extract each sentence (ending with 。 or maybe line break). We'll copy them verbatim.

Let's go through segment and extract.

I'll manually scan.

From first block (SEO) near end: "新建 Task Card 一律用既有的 `- **狀態**:` bullet 格式（照抄 `handoff/tasks/T-A2-001.md` 的接續狀態區塊），不要自創格式，否則巡查工具解析不到、等於沒寫。" Contains "一律", "不要". So we need to include whole sentence? The instruction: "只列這一段裡寫成「必須/不得/禁止/一律」的句子,原文照抄". So we need to list sentences that contain those words. Likely each sentence that contains at least one of those words.

Thus we need to capture sentences:

1. "新建 Task Card 一律用既有的 `- **狀態**:` bullet 格式（照抄 `handoff/tasks/T-A2-001.md` 的接續狀態區塊），不要自創格式，否則巡查工具解析不到、等於沒寫。"

2. From quota block: "任何「利用剩餘額度」排程都必須先回答：本輪會替 Owner 新增什麼可用能力、哪個檔案／畫面能驗證、測試是什麼、與上一輪有何 delta。答不出來就停止，不得再產生 inventory-only checkpoint。" This sentence includes "必須" and "不得". It's one sentence (maybe two sentences separated by 。 but actually there is a 。 after delta then "答不出來就停止，不得再產生 inventory-only checkpoint。" That's second sentence. So we need to split.

Let's break: First sentence: "任何「利用剩餘額度」排程都必須先回答：本輪會替 Owner 新增什麼可用能力、哪個檔案／畫面能驗證、測試是什麼、與上一輪有何 delta。" Contains "必須". Second sentence: "答不出來就停止，不得再產生 inventory-only checkpoint。" Contains "不得".

Thus two entries.

Also maybe later in same block: "封坑驗證：本機 watcher 不呼叫模型也能持續留下 used/reset/source；pre-reset automation 只有 gate=`ready` 才做一個高價值 job；`done` 沒有 output path 會被 controller 拒絕；重置後報告列出每個 output/test/commit，零成果時明確標紅而不是報成功。" Contains "不呼叫"? not imperative. No.

Third block (customer page): "Platform gate 必須拿到 platform ID、可讀 URL 與欄位 readback，file chooser `Not allowed`、登入未建立、HTTP 200 或空對話框都只記 `BLOCKED`。" Contains "必須". That's a sentence.

Also maybe "預防：每案只維護一份 `platform_metadata.md`，發布後另寫 release receipt；WordPress、長片、Short、Pin、Telegram 各自有狀態，任一平台阻擋不重做內容，也不把部分完成包成「全部上傳完成」。" Contains "不重做" and "也不把". Not "不得". So ignore.

Fourth block (Telegram): "不得用 persona 散文回答權限。" That's a sentence.

Also "Owner 問「現在」時，沒有 runtime evidence 就不得用「應該」補空白。" Contains "不得". That's a sentence.

Also "不得把 code path 當 UI 完成。" Contains "不得". That's a sentence.

Fifth block (Task Card): "不要只加新區塊了事，要順手搜整份檔案有沒有第二個 `- **狀態**:` 或 `**Status**:`**，兩個都要 reconcile 成同一份事實，否則下次改了新區塊、巡查工具還是抓到舊區塊。" Contains "不要". Also maybe later: "建議：Task Card 只允許一個「狀態」欄位，格式一律照抄 `handoff/tasks/T-A2-001.md`。" Contains "一律". That's a sentence.

Sixth block (llama-server): "不要先入為主往 prompt schema 找根因；批次腳本一律內建重試+重啟邏輯，不要假設單次呼叫必成功。" Contains "不要", "一律". This is one sentence? Actually there is a semicolon but ends with 。 after "必成功". So it's one sentence containing both.

Seventh block (model substitution): many.

- "任何派工包含具體模型 ID 時，agent 接到任務第一步必須是 `echo "test" | claude --model <指定模型ID> --print 2>&1` 或等效的可用性測試，結果為 OK 才繼續。" Contains "必須".

- "揭露替代不等於合規——合規的唯一標準是「先試指定模型，失敗才回報並等裁決」。" Not contain imperative words.

- "教訓（寫死規則）：**指定模型的任務，第一步必須實測指定模型。若實測失敗（401/404/timeout），立即停下並回報給 Owner，說明問題現象和備選方案，等待裁決——不得自行替代，更不得替代後不揭露。**". Contains "必須", "不得". This is one sentence (maybe two parts but ends with 。 after 不揭露。). Actually there is a 。 after 不揭露。 So it's one sentence.

Also maybe earlier: "解法（已驗證）：2026-07-19 實測 `CLAUDE_CODE_OAUTH_TOKEN`（bot/.env）export 後，`claude --model claude-fable-5 --print` 與 `claude --model claude-opus-4-8 --print` 均正常回應 OK，確認兩個指定模型可用。已建 `JOB-R-FABLE-VS-OPUS-VERIFIED-20260719` 用真正指定模型重跑 R01。" Not.

Eighth block (Google OAuth): maybe no imperative.

Now compile list.

Let's list each sentence exactly as appears in the segment.

We need to ensure we copy exactly, including punctuation and spaces.

I'll extract from the given text.

Segment text again (I'll copy relevant parts):

```
- 觸發條件：Owner 問「多人確認的 SEO 部門進度一直沒有回報」——實際查證，2026-07-07 SEO 三人小組派工的 4 項交付物（婚禮 pillar 整合草稿、慶生 gender-reveal 段落、B3 操作稿、cannibalization 定案表）全部已完成並 commit（`5a83f0f`），但 Owner 從未被主動告知。
- 根因（兩層）：① 完成過程只用 session 內部 task list 追蹤進度，沒有建立/更新 `handoff/tasks/T-*.md`；Owner 唯一會被主動推播的管道 `scripts/patrol-scheduled.sh`（透過 Telegram sendMessage）只掃描 `handoff/tasks/T-*.md` 裡 `- **狀態**:` 這個固定 bullet 格式欄位，沒進這個檔案 = 對 Owner 不存在。② 即使有正確格式的 Task Card，`scripts/patrol.sh` 舊版邏輯「已完成」區塊只在總數 ≤5 張時才列出檔名，超過就只顯示數字——系統長期運作下已完成 Task Card 遠超過 5 張，所以完成項目從未被實際點名過，只是被算進一個沒人看的數字。額外發現：`handoff/tasks/T-A2-SEO-CATERING-MATRIX-001.md` 用 `**Status**: X`（無 `- ` 前綴、英文欄名）而非 patrol.sh 期待的 `- **狀態**: X`，導致該卡在巡查中一直被歸類成「狀態未標記」，是同一類「格式跟解析器對不上」問題的另一個活例。
- 解法：① 新增 `scripts/notify_owner.sh`，用既有 A1 bot Telegram 憑證即時推播；② `scripts/checkpoint.sh` 新增 `--notify` flag，里程碑完成時呼叫即時推播，不必等每日 patrol；③ `scripts/patrol.sh` 的「已完成」區塊改成超過 5 張時仍列出最近異動的 3 張，不再整批消音；④ 補建 `handoff/tasks/T-A2-007-seo-trio-review-20260707.md`（正確格式，✅ 已完成）；⑤ SOP 寫進 `AGENT_RULES.md` SECTION 20，明定 WHO/WHAT 管道/HOW OFTEN。
- 預防：任何完成一個 Owner 明確派工的多步驟任務，**在同一次 checkpoint 裡就要決定要不要 `--notify`**，不要假設「有 commit 就等於有回報」——commit 只進 git 歷史，不會主動出現在 Owner 眼前。新建 Task Card 一律用既有的 `- **狀態**:` bullet 格式（照抄 `handoff/tasks/T-A2-001.md` 的接續
