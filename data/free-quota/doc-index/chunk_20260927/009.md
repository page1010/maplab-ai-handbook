> 草稿未審(free-quota 班表 20260927,model=nvidia/nemotron-3-super-120b-a12b:free,item=pitfalls.md#L421-480)。翻譯/生成稿:不得新增事實,上線前必經人審。

We need to produce index entries for this internal document snippet. The source line: "----- 檔案:pitfalls.md#L421-480 -----". So source: pitfalls.md#L421-480.

We need to split this snippet into searchable index entries. Likely each major section (date headings) corresponds to an entry. The snippet contains several sections:

- First block: Starting at line 421? Actually the snippet begins with bullet points about SEO progress. That seems like a section from 2026-07-07? Actually it's a pitfall about SEO progress not reported. Not prefixed with date but it's a pitfall.

Then there is a heading "## 2026-07-31 — 額度複利不能退化成重複 checkpoint"

Then "## 2026-08-26 — 客戶頁的日期／內部語與平台上傳成功必須分開驗證"

Then another "## 2026-08-26 — 人設手冊不是 runtime capability；Telegram 授權不能拿 chat id 當 sender id"

Then "## 2026-07-09 — 同一張 Task Card 藏兩個「狀態」欄位，且互相矛盾（第三個活例）"

Then "## 2026-07-10 — llama-server 多模態呼叫連續幾次後會退化成空輸出，與 prompt/圖片內容無關"

Then "## 2026-07-19 — 指定模型任務：自行替代模型不揭露（模擬版）+ 有揭露但仍替代（REAL版）兩種偏差模式"

Then "## 2026-08-03 — Google OAuth「測試 7 天過期」診斷指錯專案；真相來源要查活的 Console，不是紙面"

Thus each of these headings is a separate entry. Also maybe the initial block before first heading could be considered entry for the SEO progress pitfall (no date). Should we include it? The task: "把這一段拆成可搜尋的索引條目,讓其他人不必整份重讀就能找到需要的那幾行." So we should break into logical chunks. Likely each heading plus the preceding content up to that heading. So first chunk: from start up to before "## 2026-07-31". That includes the SEO progress pitfall and the解法 etc. Then each heading chunk.

Thus we will produce entries: maybe 8 entries.

For each entry we need: 條目標題 / 這一段在講什麼(30字內) / 關鍵字(3到6個,逗號分隔) / 對應行號範圍(用來源那一行的範圍,無法細分就照抄).

Since we cannot know exact line numbers for each sub-section, we must use the source range (the whole snippet) for each entry if we cannot subdivide. But we can try to infer approximate ranges? The instruction: "對應行號範圍(用來源那一行的範圍,無法細分就照抄)". So if we cannot fine-grained, we just copy the source line range: pitfalls.md#L421-480. So each entry will have same range.

We need to keep total under 500 Chinese characters. We'll produce concise entries.

Now "可執行的規定:" line after table, list sentences that contain "必須/不得/禁止/一律". We need to extract from the snippet those exact sentences.

Let's scan the snippet for such words.

From first block: "任何完成一個 Owner 明確派工的多步驟任務，**在同一次 checkpoint 裡就要決定要不要 `--notify`**，不要假設「有 commit 就等於有回報」——commit 只進 git 歷史，不會主動出現在 Owner 眼前。新建 Task Card 一律用既有的 `- **狀態**:` bullet 格式（照抄 `handoff/tasks/T-A2-001.md` 的接續狀態區塊），不要自創格式，否則巡查工具解析不到、等於沒寫。" Contains "就要決定" not exactly. Contains "必須"? Not. "一律" appears: "新建 Task Card 一律用既有的 `- **狀態**:` bullet 格式". That's a regulated sentence: "新建 Task Card 一律用既有的 `- **狀態**:` bullet 格式（照抄 `handoff/tasks/T-A2-001.md` 的接續狀態區塊），不要自創格式，否則巡查工具解析不到、等於沒寫。" That contains "一律". Also "不要自創格式" is a prohibition (不得?). It's "不要". Not exactly "不得" but maybe considered? The requirement says list sentences that are written as "必須/不得/禁止/一律". So we need sentences that contain those exact words. "一律" qualifies. Also "不要" is not listed, but maybe we treat as not. We'll include only those with those exact terms.

Second block (2026-07-31): Look for "必須/不得/禁止/一律". Sentence: "任何「利用剩餘額度」排程都必須先回答：本輪會替 Owner 新增什麼可用能力、哪個檔案／畫面能驗證、測試是什麼、與上一輪有何 delta。答不出來就停止，不得再產生 inventory-only checkpoint。" Contains "必須" and "不得". Also "封坑驗證：" maybe not. Also "pre-reset automation 只有 gate=`ready` 才做一個高價值 job；`done` 沒有 output path 會被 controller 拒絕；重置後報告列出每個 output/test/commit，零成果時明確標紅而不是報成功。" Contains "只有...才" not the keywords. So we extract sentences with those words.

Third block (2026-08-26 — 客戶頁的日期／內部語與平台上傳成功必須分開驗證): Look for "必須". Sentence: "公開前固定做兩個獨立 gate。Content gate 只掃客戶會看到的 title／body／caption／CTA／image alt，預設不曝光日期與內部流程語，WP 至少三張不同角色照片；Platform gate 必須拿到 platform ID、可讀 URL 與欄位 readback，file chooser `Not allowed`、登入未建立、HTTP 200 或空對話框都只記 `BLOCKED`。" Contains "必須". Also maybe "必須" appears elsewhere: "每案只維護一份 `platform_metadata.md`，發布後另寫 release receipt；WordPress、長片、Short、Pin、Telegram 各自有狀態，任一平台阻擋不重做內容，也不把部分完成包成「全部上傳完成」。" No keyword.

Fourth block (2026-08-26 — 人設手冊不是 runtime capability；Telegram 授權不能拿 chat id 當 sender id): Look for "必須/不得/禁止/一律". Sentence: "能力題改由 runtime deterministic readback；provider、last provider、history 與固定 actions 寫 private state。授權改查 `message.from.id`，群組另要求 @bot 或 reply。照片走 `getFile` 私密保存＋bytes/hash receipt。安全 alias 可由自然語句直接觸發；手冊快照降級為歷史背景，current 必須跑 readback。" Contains "必須". Also "不得用 persona 散文回答權限" contains "不得". Also "不得用 persona 散文回答權限". Also "任何 agent 入口都要分開記錄 `surface capability / model capability / connector capability`；不得用 persona 散文回答權限。Telegram handler 的測試矩陣至少包含 private text、group mention、group reply、non-owner、photo、unknown action；Owner 問「現在」時，沒有 runtime evidence 就不得用「應該」補空白。" Contains "不得". Also "必須" appears earlier.

Fifth block (2026-07-09 — 同一張 Task Card 藏兩個「狀態」欄位，且互相矛盾（第三個活例）): Look for "必須/不得/禁止/一律". Sentence: "建議：Task Card 只允許一個「狀態」欄位，格式一律照抄 `handoff/tasks/T-A2-001.md`。" Contains "一律". Also maybe "不要只加新區塊了事，要順手搜整份檔案有沒有第二個 `- **狀態**:` 或 `**Status**:`" contains "不要". Not keyword.

Sixth block (2026-07-10 — llama-server 多模態呼叫連續幾次後會退化成空輸出，與 prompt/圖片內容無關): Look for keywords. Sentence: "本機 Ollama vision 批次任務若開始出現空回應，**先重啟模型 process 再重跑同一筆**確認是否為此退化模式，不要先入為主往 prompt schema 找根因；批次腳本一律內建重試+重啟邏輯，不要假設單次呼叫必成功。" Contains "一律". Also "不要先入為主" etc.

Seventh block (2026-07-19 — 指定模型任務：自行替代模型不揭露（模擬版）+ 有揭露但仍替代（REAL版）兩種偏差模式): Look for keywords. Sentence: "**指定模型的任務，第一步必須實測指定模型。若實測失敗（401/404/timeout），立即停下並回報給 Owner，說明問題現象和備選方案，等待裁決——不得自行替代，更不得替代後不揭露。**". Contains "必須" and "不得". Also later: "任何派工包含具體模型 ID 時，agent 接到任務第一步必須是 `echo "test" | claude --model <指定模型ID> --print 2>&1` 或等效的可用性測試，結果為 OK 才繼續。" Contains "必須". Also "可用性測試失敗時，回報格式：「問題：指定模型 X 無法使用（錯誤：Y）；備選方案：A=等待 Owner 取得授權，B=改用 Z（但有哪些差異），C=暫停任務；請 Owner 選擇。」" no keyword. Also "揭露替代不等於合規——合規的唯一標準是「先試指定模型，失敗才回報並等裁決」." no keyword.

Eighth block (2026-08-03 — Google OAuth「測試 7 天過期」診斷指錯專案；真相來源要查活的 Console，不是紙面): Likely no keyword.

Now we need to produce entries. For each entry we need a title (條目標題). Could be the heading or a short description. We'll use the heading text as title (excluding the date maybe). For first block without heading, we can create a title like "SEO 進度未主動告知問題". Provide description within 30 Chinese characters. Provide keywords 3-6.

We'll produce maybe 8 rows.

Make sure total characters <500. We'll keep concise.

Now format:

1) 第一行寫「來源:」把「----- 檔案:」那一行的檔案與行號範圍原字照抄。

So first line: "來源: pitfalls.md#L421-480"

Then table with four columns: 條目標題 / 這一段在講什麼(30 字內) / 關鍵字(3 到 6 個,逗號分隔) / 對應行號範圍

We'll produce rows separated by newlines, maybe using markdown table? The instruction: "表格四欄:" but not specifying markdown format. We'll output like:

條目標題 | 這一段在講什麼(30字內) | 關鍵字 | 對應行號範圍
--- | --- | --- | ---
...

After table, a line: "可執行的規定:" then list sentences.

We must only include sentences that contain exactly those keywords. We'll extract them verbatim.

Let's collect sentences:

From first block:
- "新建 Task Card 一律用既有的 `- **狀態**:` bullet 格式（照抄 `handoff/tasks/T-A2-001.md` 的接續狀態區塊），不要自創格式，否則巡查工具解析不到、等於沒寫。"

From 2026-07-31 block:
- "任何「利用剩餘額度」排程都必須先回答：本輪會替 Owner 新增什麼可用能力、哪個檔案／畫面能驗證、測試是什麼、與上一輪有何 delta。答不出來就停止，不得再產生 inventory-only checkpoint。"
Also maybe "pre-reset automation 只有 gate=`ready` 才做一個高價值 job；`done` 沒有 output path 會被 controller 拒絕；重置後報告列出每個 output/test/commit，零成果時明確標紅而不是報成功。" does not contain keywords.

From 2026-08-26 — 客戶頁 block:
- "Platform gate 必須拿到 platform ID、可讀 URL 與欄位 readback，file chooser `Not allowed`、登入未建立、HTTP 200 或空對話框都只記 `BLOCKED`。"

From 2026-08-26 — 人設手冊 block:
- "current 必須跑 readback。"
- "不得用 persona 散文回答權限"
- "不得用 persona 散文回答權限" appears twice? Actually there are two: one about not using persona; second about not using "應該". Let's capture both:
   - "不得用 persona 散文回答權限"
   - "不得用「應該」補空白"
Also "任何 agent 入口都要分開記錄 `surface capability / model capability / connector capability`；不得用 persona 散文回答權限。" Actually that sentence includes both "必須"? It has "都要分開記錄" not keyword. It contains "不得用 persona 散文回答權限". So we can include that sentence as well: "任何 agent 入口都要分開記錄 `surface capability / model capability / connector capability`；不得用 persona 散文回答權限。"
Also "Telegram handler 的測試矩陣至少包含 private text、group mention、group reply、non-owner、photo、unknown action；Owner 問「現在」時，沒有 runtime evidence 就不得用「應該」補空白。" Contains "不得". So include that.

From 2026-07-09 block:
- "建議：Task Card 只允許一個「狀態」欄位，格式一律照抄 `handoff/tasks/T-A2-001.md`。"

From 2026-07-10 block:
- "批次腳本一律內建重試+重啟邏輯，不要假設單次呼叫必成功。"
Also maybe "本機 Ollama vision 批次任務若開始出現空回應，**先重啟模型 process 再重跑同一筆**確認是否為此退化模式，不要先入為主往 prompt schema 找根因；" contains "不要". Not keyword.

From 2026-07-19 block:
- "**指定模型的任務，第一步必須實測指定模型。若實測失敗（401/404/timeout），立即停下並回報給 Owner，說明問題現象和備選方案，等待裁決——不得自行替代，更不得替代後不揭露。**"
- "任何派工包含具體模型 ID 時，agent 接到任務第一步必須是 `echo "test" | claude --model <指定模型ID> --print 2>&1` 或等效的可用性測試，結果為 OK 才繼續。"
Also maybe "可用性測試失敗時，回報格式：「問題：指定模型 X 無法使用（錯誤：Y）；備選方案：A=等待 Owner 取得授權，B=改用 Z（但有哪些差異），C=暫停任務；請 Owner 選擇。」" no keyword.
Also "揭露替代不等於合規——合規的唯一標準是「先試指定模型，失敗才回報並等裁決」." no keyword.

From 2026-08-03 block: none.

Now compile list, ensure we don't duplicate.

Let's list unique sentences:

1. 新建 Task Card 一律用既有的 `- **狀態**:` bullet 格式（照抄 `handoff/tasks/T-A2-001.md` 的接續狀態區塊），不要自創格式，否則巡查工具解析不到、等於沒寫。

2. 任何「利用剩餘額度」排程都必須先回答：本輪會替 Owner 新增什麼可用能力、哪個檔案／畫面能驗證、測試是什麼、與上一輪有何 delta。答不出來就停止，不得再產生 inventory-only checkpoint。

3. Platform gate 必須拿到 platform ID、可讀 URL 與欄位 readback，file chooser `Not allowed`、登入未建立、HTTP 200 或空對話框都只記 `BLOCKED`。

4. current 必須跑 readback。

5. 不得用 persona 散文回答權限。

6. 任何 agent 入口都要分開記錄 `surface capability / model capability / connector capability`；不得用 persona 散文回答權限。

7. 不得用「應該」補空白。

8. Telegram handler 的測試矩陣至少包含 private text、group mention、group reply、non-owner、photo、unknown action；Owner 問「現在」時，沒有 runtime evidence 就不得用「應該」補空白。

9. 建議：Task Card 只允許一個「狀態」欄位，格式一律照抄 `handoff/tasks/T-A2-001.md`。

10. 批次腳本一律內建重試+重啟邏輯，不要假設單次呼叫必成功。

11. 指定模型的任務，第一步必須實測指定模型。若實測失敗（401/404/timeout），立即停下並回報給 Owner，說明問題現象和備選方案，等待裁決——不得自行替代，更不得替代後不揭露。

12. 任何派工包含具體模型 ID 時，agent 接到任務第一步必須是 `echo "test" | claude --model <指定模型ID> --print 2>&1` 或等效的可用性測試，結果為 OK 才繼續。

Check each for keyword presence: "必須"/"不得"/"禁止"/"一律". Sentence 10 has "一律". Good.

Now we need to output them under "可執行的規定:" line, each sentence on its own line? Probably list them.

Now produce table entries.

We need to decide titles for each chunk.

Chunk 1 (SEO progress): Title: "SEO 進度未主動告知". Description: "Owner 未被告知 SEO 三人小組已完成交付物，因未更新任務卡及巡查工具只讀特定格式". Within 30 Chinese characters. Let's craft: "Owner 未獲 SEO 任務完成通知，因任務卡格式錯誤". That's maybe 22 characters.

Keywords: SEO, 任務卡, 巡查工具, 未告知. Need 3-6. We'll give: SEO,任務卡,巡查工具,未告知 (4).

Row.

Chunk 2 (2026-07-31 quota): Title: "額度複利不能退化成重複 checkpoint". Description: "利用剩餘額度需先評估增值，避免只做重複檢查點". 30 chars: "利用餘額必須先評估增值，禁重複 checkpoint". Keywords: 額度,增值,checkpoint,不得.

Chunk 3 (2026-08-26 客戶頁): Title: "客戶頁日期與內部語分離驗證". Description: "公開前需分別檢查客戶可見內容與平台上傳狀態". 30 chars: "公開前分別驗證客戶內容與平台上傳". Keywords: 客戶頁,平台上傳,必須.

Chunk 4 (2026-08-26 人設手冊): Title: "Telegram 授權與能力驗證規範". Description: "授權必須查詢 message.from.id，能力需 runtime 驗證". 30 chars: "Telegram 授權用 message.from.id，能力需 runtime". Keywords: Telegram,授權,能力,必須.

Chunk 5 (2026-07-09 Task Card 狀態重複): Title: "Task Card 單一狀態欄位規範". Description: "任務卡只允許一個狀態欄位，格式必須統一". 30 chars: "Task Card 只准一個狀態欄位，格式統一". Keywords: Task Card,狀態欄位,一律.

Chunk 6 (2026-07-10 llama-server 多模態退化): Title: "多模態視覺任務重試機制". Description: "視覺批次任務出現空回應需重啟模型並內建重試". 30 chars: "視覺任務空回應需重啟並重試". Keywords: llama-server,重試,一律.

Chunk 7 (2026-07-19 指定模型任務): Title: "指定模型必須先實測不可自行替代". Description: "使用指定模型前必須先測試可用，失敗則回報等裁決". 30 chars: "指定模型先測試，失敗回報等裁決". Keywords: 指定模型,必須,不得.

Chunk 8 (2026-08-03 Google OAuth): Title: "Google OAuth 過期診斷需查活 Console". Description: "過期問題需查實際 Google Cloud Console，而非紙面文件". 30 chars: "OAuth 過期診斷查活 Console". Keywords: Google OAuth,過期,Console.

Now produce table rows with same source range.

We need to keep within 500 characters total. Let's estimate.

Each row: title (~10-20) + description (~30) + keywords (~20) + range (like "pitfalls.md#L421-480") (~20). plus separators. Rough estimate per row ~100 chars. 8 rows => 800 >500. Need to be shorter. Maybe we need fewer entries. Could combine some? But instruction says split into searchable index entries; maybe each heading is an entry, that's 8 entries indeed. Need to keep total <500 characters. Let's try to make each entry very short.

We can compress:
