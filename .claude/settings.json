{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'echo \"$CLAUDE_TOOL_INPUT\" | grep -qiE \"(password|secret|token|api_key|private_key|-----BEGIN|credential|AKIA[0-9A-Z]{16})\" && echo \"[BLOCKED] 疑似 credential 洩漏，已攔截\" && exit 1 || exit 0'"
          }
        ]
      }
    ]
  }
}
