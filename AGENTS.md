# MAPLAB Codex entry

Start from the shared Claude/Codex work view:

1. Read `/Users/pagemacmini/claude-daily-operations/state/CROSS_AGENT_START_HERE.md`.
2. Run:

```bash
python3 /Users/pagemacmini/claude-daily-operations/ops/claude-daily-operations/cross_agent_start.py \
  --agent codex \
  --repo /Users/pagemacmini/maplab-ai-handbook
```

3. Read MAPLAB `CURRENT_STATUS.md`, `pitfalls.md`, the selected Task Card, and its latest receipt.
4. Claim the registered bounded action, continue from the existing artifact, and checkpoint a durable receipt before handoff.

The shared workset shows recent commits and dirty paths so interrupted Claude work is visible before Codex edits overlapping files. Live services remain verified through their authenticated surface.
