> 草稿未審(free-quota 班表 20260926,model=nvidia/nemotron-3-super-120b-a12b:free,item=pitfalls.md#L601-660)。翻譯/生成稿:不得新增事實,上線前必經人審。

We need to produce output per instructions. First, extract source line: "----- 檔案:pitfalls.md#L601-660 -----". So source: that line.

Then we need to split the segment into searchable index entries. Likely each major heading (like each dated section) is an entry. We need entry title, brief description (30 chars), keywords (3-6), and line range (use source range if cannot subdivide). We could try to subdivide based on headings: each dated section includes a title line like "## 2026-08-27 — Durable supervisor 必須從逐筆不可變證據重算完成，不可信任 summary". Could treat each as entry. Also there is earlier bullet list before first date (the pitfalls about LINE). That could be an entry.

We need to keep within 500 chars total. Probably a few entries.

Let's identify sections:

1) Opening bullet list before first date: from line start until before "## 2026-08-27 — Durable supervisor ..." maybe lines 601-... We'll just assign same range.

2) 2026-08-27 — Durable supervisor 必須從逐筆不可變證據重算完成，不可信任 summary

3) 2026-08-27 — Shell 搜尋字串中的 backtick 仍會被執行

4) 2026-08-27 — Durable job 與私有 worker receipt 是兩個 ownership root，驗證工具不可混用

5) 2026-08-27 — Live 服務頁與案例資料夾都不能單獨證明真實案例

6) 2026-08-28 — 多跑不同樣本不等於訓練，也不等於換方法

7) 2026-08-28 — Durable resume 不可依賴呼叫者記得私有 data root

8) 2026-08-28 — 找到客戶要求不等於找到可收費漏損；缺 join key 時應停掉分類迴圈

That seems 8 entries.

Now need brief description within 30 Chinese characters. Let's craft.

Entry 1: "LINE job 隱私風險與防護措施". Count characters: L I N E (4) + space? Actually Chinese characters count: "LINE" considered as 4 letters? We'll just keep short. Need <=30. We'll do: "LINE job 隱私風險與防護措施". That's maybe ~20.

Keywords: LINE, 隱私, 防護, deidentification, cache (3-6). Choose: LINE, 隱私, 防護, deidentification, cache (5).

Line range: source range 601-660.

Entry 2: Title: "Durable supervisor 必須重算完成不可信任 summary". Description: "要求以不可變逐筆證據重算完成狀態". Count maybe 20. Keywords: supervisor, 完成, 不可變, 證據, 重算 (5). Line range same.

Entry 3: Title: "Shell 搜尋字串中的 backtick 執行風險". Description: "雙引號內 backtick 會被 command substitution 執行". Keywords: Shell, backtick, command substitution, rg, 安全 (5). Line range same.

Entry 4: Title: "Durable job 與私有 receipt 雙根驗證". Description: "job lock 與 user-local receipt 必分別驗證". Keywords: job, receipt, ownership root, 驗證, 分離 (5). Line range same.

Entry 5: Title: "Live 頁與案例夾不可單獨證明真實案例". Description: "需逐案對應資料夾、身分、素材才算案例證據". Keywords: Live, 案例, 資料夾, 身分, 證據 (5). Line range same.

Entry 6: Title: "多跑不同樣本不等於訓練或換方法". Description: "隨機換樣本不構成訓練，需固定變因比較". Keywords: 訓練, 樣本, 變因, baseline, stop-loss (5). Line range same.

Entry 7: Title: "Durable resume 必須從 receipt 取得 data root". Description: "resume 時不可依賴呼叫者記憶，必須從 receipt 還原". Keywords: resume, data-root, receipt, 私有資料, 驗證 (5). Line range same.

Entry 8: Title: "缺 join key 時應停止分類迴圈". Description: "找到客戶要求不等於可收費漏損，需四柱 join 完成才計". Keywords: join key, 漏損, 四柱, 停止, 證據 (5). Line range same.

Now need to output format:

First line: 來源: ----- 檔案:pitfalls.md#L601-660 -----

Then table with four columns: 條目標題 / 這一段在講什麼(30字內) / 關鍵字(3到6個,逗號分隔) / 對應行號範圍

We need to separate columns with maybe " / ". We'll produce each entry line.

After table, a line "可執行的規定:" then list sentences that contain 必須/不得/禁止/一律 from the segment, verbatim.

We need to extract those sentences. Let's scan the text for those keywords.

From first bullet list: "LINE job 永遠標 `private-local-only`;" not containing those words. "cache 移到 user-local 0700 目錄、檔案 0600，只接受固定 `http://127.0.0.1:11434/api/generate`，child 移除 cloud keys/proxy，receipt 明列 `external_network_calls=0` 與 loopback calls." none. "預防：deidentification gate 必逐欄檢查直接識別、準識別與語意重識別，不可只看姓名。" contains "必" and "不可". So we have "deidentification gate 必逐欄檢查直接識別、準識別與語意重識別，不可只看姓名。" That's a sentence. Also "未經新 Owner 授權，private corpus 不得因第三方宣稱 ZDR/free 而改走雲端。" contains "不得". Good.

Next section: "Durable supervisor 必須從逐筆不可變證據重算完成，不可信任 summary". Contains "必須" and "不可". Actually the sentence: "Durable supervisor 必須從逐筆不可變證據重算完成，不可信任 summary". We'll capture that phrase? The sentence: "Durable supervisor 必須從逐筆不可變證據重算完成，不可信任 summary". It includes "必須" and "不可". We'll capture as is.

Also later: "解法：加入 job-scoped `flock`、鎖內 reread、stale-writer CAS、canonical transition matrix、immutable qualification contract/seed schedule、run/receipt/delta ID+SHA-256、防重播、exact seed/stage/batch binding，並由 `results[].evaluation` 重算 pass、mean 與 unsupported aggregates。" Not containing keywords.

"預防：任何可自動進 `COMPLETED` 的 supervisor 都要把 completion 視為安全邊界；狀態只能由不可變逐筆證據導出。錯誤 terminalization 必走同一 lock/CAS，lock busy 只讀回 running，不得寫入。" Contains "都要", "只能", "必走", "不得". So we have sentences: "任何可自動進 `COMPLETED` 的 supervisor 都要把 completion 視為安全邊界；" includes "都要". Also "狀態只能由不可變逐筆證據導出。" includes "只能". Also "錯誤 terminalization 必走同一 lock/CAS，lock busy 只讀回 running，不得寫入。" includes "必走" and "不得". We'll capture each clause maybe as separate? But requirement: list sentences that contain those words. Could list each sentence. We'll just capture each sentence that contains at least one keyword.

Next section: "Shell 搜尋字串中的 backtick 仍會被執行". Contains "仍會". Not keyword. But solution: "含 Markdown backtick 或 `$` 的搜尋 pattern 一律用單引號包住，或改為固定字串參數；" contains "一律". Also "失敗後立即檢查 command output，確認沒有敏感值或寫入副作用。" contains "立即". Not keyword. "預防：建構 shell command 前先做 interpolation audit；未知／外來文字不要直接插入 command string。" contains "不要". Not keyword. "驗證搜尋使用 `rtk rg -n 'pattern-with-`backtick`' ...`，不得讓 shell 先解讀 pattern。" contains "不得". So capture that sentence.

Next: "Durable job 與私有 worker receipt 是兩個 ownership root，驗證工具不可混用". Contains "不可". Also solution: "canonical `job.json` 與 `.line-training-supervisor.lock` 用 repo path 驗證；user-local run、delta、receipt 用 `stat`／SHA-256 驗證。`git status` 只接 repo 內 path，不拿它查外部資料根。" Contains "只". Not keyword maybe "只". Not required? The keywords are 必須/不得/禁止/一律. "只" not in list. So ignore. Prev: "預防：每個 durable adapter 的 Resume Prompt 必列出 control root、data root、lock path 與各自驗證工具；完成檢查先按 ownership root 分組，不用單一命令跨兩個根。" Contains "必". So capture.

Next: "Live 服務頁與案例資料夾都不能單獨證明真實案例". Contains "不能". Not keyword. Solution: "案例產線固定走 `Drive folder ID/inventory → event/quote anchor → TimeTree/外燴系統 → ASSET_LOG file ID → visual QA → 公開來源三角 → live SEO collision/pillar route → title/keyword/style`；無關私密文件標記後排除，不引用或摘要。" Contains "不引用". Not keyword. "預防：任何案例 registry 先跑 `scripts/maplab_case_first_gate.py --level intake`；進 WP 前再跑 `--level wp --case-id ...`。服務頁不能作 `source_kind`，final keyword 必須同時有 verified identity 與 live collision proof。" Contains "不能", "必須". So capture "服務頁不能作 `source_kind`，final keyword 必須同時有 verified identity 與 live collision proof。" Also maybe "任何案例 registry 先跑 ..." not keyword.

Next: "多跑不同樣本不等於訓練，也不等於換方法". Contains "也不". Not keyword. Solution: "Supervisor 新增 plateau guard；兩個未通過 qualification rounds 後切到 `method-redesign`，後續 resume 零模型呼叫、零 attempt。下一版先固定 20 案分層 canary、校正 rubric，再做 baseline/candidate 單一變因比較。" Contains "先". Not keyword. "預防：每次新實驗必填 hypothesis、failure bucket、changed variable、fixed holdout、expected delta、stop-loss 與 method version；同方法兩輪無改善後禁止只換 seed／樣本繼續跑。第三次同錯必跑第一性原理 5 題並更新 regression set。" Contains "必填", "禁止", "必跑". So capture sentences with those words: "每次新實驗必填 hypothesis、failure bucket、changed variable、fixed holdout、expected delta、stop-loss 與 method version；" includes "必填". Also "同方法兩輪無改善後禁止只換 seed／樣本繼續跑。" includes "禁止". Also "第三次同錯必跑第一性原理 5 題並更新 regression set。" includes "必跑".

Next: "Durable resume 不可依賴呼叫者記得私有 data root". Contains "不可". Solution: "resume 先驗證 owner-only supervisor receipt，再從 receipt 的 `data_root` 綁回相同 private dataset；CLI explicit value 只保留初始設定／診斷用途，receipt path 與 job id／data root 必須一致。" Contains "只保留". Not keyword. "必須一致". Contains "必須". So capture "receipt path 與 job id／data root 必須一致。" Also "預防：任何 durable job 的 provider、data root、model digest 與 contract 都要由 canonical receipt 自我恢復；環境變數只能 bootstrap，不能成為跨 session 單一真相源。" Contains "都要", "只能", "不能". So capture sentences: "任何 durable job 的 provider、data root、model digest 與 contract 都要由 canonical receipt 自我恢復；" includes "都要". Also "環境變數只能 bootstrap，不能成為跨 session 單一真相源。" includes "只能", "不能".

Next: "找到客戶要求不等於找到可收費漏損；缺 join key 時應停掉分類迴圈". Contains "應". Not keyword. Solution: "停止增加 keyword、round 或 classifier 版本；先建立本機 read-only join bridge，把 case、quote、charge、asset 的最小 key 在 process 內對應，receipt 只留 hashes、evidence status 與 missing codes。Live readback 若仍 zero stable joins，就立即產 field-level schema proposal，並把下一樣本改為「已有 quote＋charge 的 order 往回找 conversation」，不再對相同 random conversations 加模糊條件。四柱缺一就維持 `insufficient_evidence`、金額 0。" Contains "只留". Not keyword. "立即". Not keyword. "不再". Not keyword. "就維持". Not keyword. Prev: "預防：margin-leak pipeline 的第一個 acceptance 不是「候選數」，而是四柱 join coverage；每案必有 baseline scope、actual delivery、incremental cost、charged fee。實驗抽樣要先看 evidence availability：要找漏收金額時優先從 evidence-rich orders 做 join-first，conversation-first 只適合訊號 taxonomy。Pointer、keyword、姓名或回覆語氣只能進 review queue，不可自動成為 leakage。" Contains "必有", "要先", "只能", "不可". So capture sentences with those keywords: "每案必有 baseline scope、actual delivery、incremental cost、charged fee。" includes "必有". "實驗抽樣要先看 evidence availability：" includes "要先". "Pointer、keyword、姓名或回覆語氣只能進 review queue，" includes "只能". "不可自動成為 leakage。" includes "不可".

Now we need to list these sentences verbatim under "可執的規定:". Probably each on new line, maybe bullet? The instruction: "只列這一段裡寫成「必須/不得/禁止/一律」的句子,原文照抄,沒有就寫「本段無」." So we just list each sentence that contains those words. Should we include duplicates? Probably list each distinct sentence. We'll compile.

Let's gather them:

1. "deidentification gate 必逐欄檢查直接識別、準識別與語意重識別，不可只看姓名。"
2. "未經新 Owner 授權，private corpus 不得因第三方宣稱 ZDR/free 而改走雲端。"
3. "Durable supervisor 必須從逐筆不可變證據重算完成，不可信任 summary"
4. "任何可自動進 `COMPLETED` 的 supervisor 都要把 completion 視為安全邊界；"
5. "狀態只能由不可變逐筆證據導出。"
6. "錯誤 terminalization 必走同一 lock/CAS，lock busy 只讀回 running，不得寫入。"
7. "含 Markdown backtick 或 `$` 的搜尋 pattern 一律用單引號包住，或改為固定字串參數；"
8. "驗證搜尋使用 `rtk rg -n 'pattern-with-`backtick`' ...`，不得讓 shell 先解讀 pattern。"
9. "預防：每個 durable adapter 的 Resume Prompt 必列出 control root、data root、lock path 與各自驗證工具；"
10. "服務頁不能作 `source_kind`，final keyword 必須同時有 verified identity 與 live collision proof。"
11. "每次新實驗必填 hypothesis、failure bucket、changed variable、fixed holdout、expected delta、stop-loss 與 method version；"
12. "同方法兩輪無改善後禁止只換 seed／樣本繼續跑。"
13. "第三次同錯必跑第一性原理 5 題並更新 regression set。"
14. "receipt path 與 job id／data root 必須一致。"
15. "任何 durable job 的 provider、data root、model digest 與 contract 都要由 canonical receipt 自我恢復；"
16. "環境變數只能 bootstrap，不能成為跨 session 單一真相源。"
17. "每案必有 baseline scope、actual delivery、incremental cost、charged fee。"
18. "實驗抽樣要先看 evidence availability："
19. "Pointer、keyword、姓名或回覆語氣只能進 review queue，"
20. "不可自動成為 leakage。"

Check if any missing: also from first bullet list: "LINE job 永遠標 `private-local-only`;" does not contain keywords. "cache 移到 ..." none. "只接受固定 ..." none. "child 移除 cloud keys/proxy" none. "receipt 明列 `external_network_calls=0` 與 loopback calls." none. "未經新 Owner 授權，private corpus 不得..." we have.

Also "解法：..." none.

Now ensure each sentence ends with appropriate punctuation as in source. We'll copy exactly.

Now produce table.

We need to keep total under 500 chars. Let's estimate.

Source line: maybe ~30 chars.

Each table line: Title (maybe ~20) + description (<=30) + keywords (~20) + range (like 601-660). Approx 100 chars per line. 8 lines => 800 >500. Need to be concise. Perhaps we should limit entries to fewer? But requirement:把這一段拆成可搜尋的索引條目,讓其他人不必整份重讀就能找到需要的那幾行。 Could be many entries but must stay under 500 chars total. So we need to be very concise.

We need to keep total output <500 characters. Let's aim for maybe 4 entries.

But we need to capture the main ideas. Could combine multiple sections into one entry? But each entry corresponds to a段落? The instruction: "把這一段拆成可搜尋的索引條目" - we can split as we like, but must stay under 500 chars.

Let's try 5 entries with short fields.

We need to include source line.

Let's count characters roughly.

We'll produce each line like: "1) 標題 / 描述 / 關鍵字 / 行號". Need to count.

Assume each line ~80 chars. 5 lines => 400 + source line ~30 => 430. Good.

Let's craft 5 entries covering major topics:

Entry1: LINE job隱私風險
Entry2: Durable supervisor 必須重算完成
Entry3: Shell backtick 執行風險
Entry4: Durable job 與 receipt 雙根驗證
Entry5: 其他 (maybe combine rest) but we need to stay under limit.

We can combine remaining topics into one entry: "多跑樣本、Resume、案例證據、join key". That's okay.

Let's craft.

Entry1 Title: "LINE job 隱私風險與防護"
Desc: "姓名替換易誤標為deidentified，需私屬快取與欄位檢查". Count Chinese characters: maybe 30? Let's count: LINE(4) space? We'll just approximate. We'll ensure <=30.

Keywords: LINE, 隱私, 防護, deidentification, cache (5)

Line range: 601-660

Entry2 Title: "Durable supervisor 必須重算完成"
Desc: "完成狀態必須由不可變逐筆證據重算，不可信任summary". Keywords: supervisor, 完成, 不可變, 證據, 重算 (5)

Entry3 Title: "Shell backtick 執行風險"
Desc: "雙引號內backtick會被命令替換執行，需單引號或固定參數". Keywords: Shell, backtick, command substitution, rg, 安全 (5)

Entry4 Title: "Durable job 與 receipt 雙根驗證"
Desc: "job lock 用repo路徑驗證，user-local receipt 用stat/SHA-256驗證，git status只限repo內". Keywords: job, receipt, 雙根, 驗證, 路徑 (5)

Entry5 Title: "其他規範：實驗、Resume、案例、join"
Desc: "實驗必填假設、禁止換seed；Resume需從receipt取data-root；案例需逐案證據；缺joinkey停分類". Keywords: 實驗, Resume, 案例, join key, 禁止 (5)

Now we need to ensure each description <=30 Chinese characters. Let's count roughly.

Entry1 desc: "姓名替換易誤標為deidentified，需私屬快取與欄位檢查". Count characters: 姓名(2) 替換(2) 易(1) 誤標(2) 為(1) deidentified(?? treat as one word maybe count as 10?) but we count Chinese characters only? Might exceed. Let's shorten: "姓名替換易誤標，需私屬快取與欄位檢查". Count: 姓名2 替換2 易1 誤標2 需1 私屬2 快取1 與1 欄位2 檢查2 = total? Let's just trust it's under 30.

Entry2 desc: "完成狀態必須由不可變逐筆證據重算，不可信任summary". Count maybe okay.

Entry3 desc: "雙引號內backtick會被命令替換執行，需單引號或固定參數". Count maybe okay.

Entry4 desc: "job lock 用repo路徑驗證，user-local receipt 用stat/SHA-256驗證，git status只限repo內". Count maybe okay.

Entry5 desc: "實驗必填假設、禁止換seed；Resume需從receipt取data-root；案例需逐案證據；缺joinkey停分類". Count maybe okay.

Now produce output.

First line: 來源: ----- 檔案:pitfalls.md#L601-660 -----

Then table lines with numbers.

We need to format: 1) 條目標題 / 這一段在講什麼(30字內) / 關鍵字(3到6個,逗號分隔) / 對應行號範圍

We'll follow that.

Let's craft each line.

Line1: "1) LINE job 隱私風險與防護 / 姓名替換易誤標，需私屬快取與
