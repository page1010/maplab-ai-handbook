> 草稿未審(free-quota 班表 20260924,model=nvidia/nemotron-3-super-120b-a12b:free)。翻譯/生成稿:不得新增事實,上線前必經人審。

We need to list pending tasks from the provided internal documents. Must only use content from files. Must list pending tasks (those not completed). Need to read each file content for tasks with status not completed (e.g., open, pending, waiting for Owner, etc). Then output per instructions: first line "本次依據檔案:" listing the filenames actually read. Then a table with three columns: 待辦事項 / 文件中的狀態字樣(原文照抄) / 出自哪一個檔案. Then after table, a paragraph "文件沒有交代的部分" listing items where file only gave title but no status, or contradictory statuses.

We need to read the provided files: AGENT_CORE.md, TASK_QUEUE.md, CURRENT_STATUS.md#tail500 (i.e., we only have the tail part of CURRENT_STATUS.md). The content we have is the tail 500 lines (starting from "# CURRENT_STATUS.md — 唯一最新狀態入口" etc). We need to extract pending tasks from these.

We must not infer beyond what is written. We must only list tasks that are indicated as pending/uncompleted.

We need to scan each file for tasks and their status.

Let's parse each file.

--- AGENT_CORE.md ---

This file is mostly description of company, no explicit tasks list. There's a section about "為什麼這一段要寫在最前面" etc. No task statuses. So no pending tasks from AGENT_CORE.md.

--- TASK_QUEUE.md ---

This file contains tasks with status indicators.

We need to extract tasks that are not completed (i.e., status not done/completed). Look at sections: Tier 1, Tier 2, FROZEN, plus maybe "本階段已交付（近期）" which are completed. Also "Owner 自己接" and "Owner 暫緩". Those might be pending? The "Owner 自己接" lists tasks that Owner should do; likely pending unless completed? Not indicated as completed. Similarly "Owner 暫緩" are pending but on hold.

We must list pending tasks as per file.

Let's parse TASK_QUEUE.md content:

We have:

## 🔴 Tier 1 — 立即執行（3 步內直連現金流 / 解鎖 Owner 決策）

Table:

| 任務 | 負責 | 狀態 | 下一步 | Owner 阻塞點 |
|------|------|------|--------|------------|
| **A6 LINE webhook 上線** — T-A6-001 | A6 | 🔴 CRITICAL（~275h） | Owner 填入 Webhook URL → A6 bot 立即開始收 LINE 訊息 | ✋ 需 Owner 操作 LINE Developers Console（Channel 1654658337）填 Webhook URL，5 分鐘內可解 |
| **B3 廣告試跑點火** — B3-ADCOPY | B3 | ⏸️ 等 Owner 操作 | 操作稿已備 `docs/runbooks/2026-07-07-b3-trial-launch-stepbystep.md`，Owner 在 Meta Ads Manager（帳號 318634712）建立受眾包即可啟動 | ✋ 需 Owner 登入 Meta Ads Manager，無 API，需人工建立冷層受眾包 |
| **IS 規則引擎核准包** — IS-Layer3 | B1 | ⏸️ 等 Owner 確認 4 參數 | 規則引擎本體已跑（5 條 WARNING 在 escalation_queue），只差 Owner 確認門檻參數 → B1 建 escalation_push.sh | ✋ 需 Owner 確認：SOP1 主題集中度上限（草稿 10%）、SOP2 單標的上限（草稿 15%）、槓桿警戒線（草稿 1.5x）、日跌幅急性警示（草稿未定義） |
| **A7/Hermes LINE→Sheets 邊界重設** — T-A7-001 / T-A6-HERMES-LINE-GYM-001 | A7/A6 | 🟡 LOCAL CONTRACT PASS / LIVE NOT DEPLOYED | 先檢視 `docs/hermes-line-sheets-assistant-flow-v1.md` 與 `*` 樣板；獲授權後只在隔離測試部署 neutral Sheet actions | ✋ live GAS／LINE 測試需 Owner 明示授權；舊 `data/a7-reply-templates.md` 已降為 LEGACY，禁止直接上線 |
| **A2 廣告現況巡查 + 關鍵字/行銷規劃** — T-A2-006 | A2 | 🟢 不再等 MCP token | 走 `skills/ad-platform-browser-check.md`（瀏覽器唯讀巡查 Meta/Google Ads 現況：活動/受眾/素材），彙整結果餵進關鍵字與行銷整體規劃 | 無（唯讀巡查不需 Owner 操作，需在有 Chrome MCP 的環境如 Mac mini/Cowork 執行） |

So pending tasks: those not completed: all tasks except maybe the last one if status indicates done? Status: 🟢 不再等 MCP token — suggests it's ready to execute, not yet done? It says "不再等 MCP token", meaning it's not waiting for token, but hasn't been executed? The description says "走 ... 唯讀巡查...", likely still pending execution. So we consider it pending.

Thus Tier 1 tasks pending: A6 LINE webhook 上線, B3 廣告試跑點火, IS 規則引擎核准包, A7/Hermes LINE→Sheets 邊界重設, A2 廣告現況巡查 + 關鍵字/行銷規劃.

Now Tier 2:

## 🟡 Tier 2 — 次輪執行（有槓桿待上游解鎖）

Table:

| 任務 | 負責 | 狀態 | 解鎖條件 |
|------|------|------|---------|
| A7 QA 迭代（隔離測試後） | A7 | 🔲 待 live 測試授權 | neutral Sheet shell 回讀無商務欄位後，再由 Mina 回傳首週草稿微調 |
| A5 外送費級距建立（Q5 自動計算） | A5 | 🔲 待 Owner 定規格 | Owner 給外送費 base rate / 級距表 |
| A2 SEO 草稿發布（婚禮 pillar + gender-reveal） | A2 | ⏸️ 等 Owner 核准 | Owner review `workbook/outputs/seo-gap-drafts/` 草稿後授權 WP 發布 |
| A2/A3 Round 008 圖片插入 + 發布 | A2/A3 | ⏸️ 阻塞（Chrome extension file chooser Not allowed） | Owner 開啟 Codex Chrome extension file URL access |
| T-HQ-001 三個 launchctl load | HQ | ⏳ 代碼完成，等 Owner 執行 | Owner 跑三個 `launchctl load` 指令（見 T-HQ-001.md） |
| weekly-eval-compounding 修復（Owner 5247 追問，2026-09-14 開卡） | A0/Fable5 | 🔴 連三週失敗（08-30 timeout、09-06/09-14 Codex models cache 壞：missing field base_instructions；且 TELEGRAM token 未設=失敗通知靜默） | 清 Codex models cache 或升級 codex-cli 後手動重跑 `scripts/weekly_eval_compounding.py` 驗證；補 launchd plist 的 TELEGRAM_BOT_TOKEN/CHAT_ID 環境變數讓失敗會叫人；09-21（週一 01:00）排程驗收 |

So pending tasks: all these (since status indicates waiting). The last one has failures but still pending fix.

Now FROZEN:

## 🧊 FROZEN — 凍結（算力空轉，等解鎖條件）

Table:

| 任務 | 凍結原因 | 解鎖條件 |
|------|---------|---------|
| T-A2-002 foodsafety post 698 改法 | 等 Owner 決定改法，現有自動防護已上 | Owner 決定「無麩質 FAQ」答案改法 |
| T-A2-003 weekly WP audit 排程 | 等 Owner 建排程 | Owner 執行 `launchctl load` |
| T-A6-002 LINE 訓練資料收集 | 方向未定，依賴 T-A6-001 先上線 | A6 LINE webhook 上線後重評 |
| T-GBP-001 GBP 新圖 | 等 Owner 準備新圖 | Owner 提供新照片 |
| T-A5-002/T-A5-007 Codex 接手 | awaiting Owner 驗證 ~472h | Owner review Codex 輸出 |
| A8 影音上傳（IG/YouTube） | 等 Owner/A1 approval | Owner 核准 review draft |
| Investment OS B3 Archivist 例行維護 | B2-B4 由 Ollama 接手，Claude 待 escalation | Ollama 地端跑不動才 escalate |
| Antigravity ExecutionLease/Ontology 提案（`docs/governance/antigravity-execution-lease-ontology-v0.1.md`） | ①需 Codex 確認原始設計是否已存檔 ②agy sandbox 唯讀保證未驗證（`skills/codex-offload-guide.md` 2026-07-06 缺口至今未關）③是否擴大 Antigravity 角色屬於一次性升格決定，需 Owner 明確核准 | Codex 存檔原始設計 + agy sandbox 驗證通過 + Owner 對角色升格拍板 |

Thus pending tasks: all these frozen tasks.

Now also there is "✅ 本階段已交付（近期）" list of completed items; we should not list those as pending.

Also "👤 Owner 自己接" and "⏸ Owner 暫緩" sections:

- Owner 自己接:
  - 富途牛牛即時訊息 API（起點 script 已給）
  - B3 Meta 廣告受眾包建立（操作稿已備，需 Owner 人工操作）

These are tasks Owner should do; status not indicated as completed. Likely pending.

- Owner 暫緩:
  - Notion 鑰匙搬遷（重授權或搬本機）
  - A2 個人品牌 B2B/B2C 策略（雜談）

These are pending but on hold.

Thus we need to list pending tasks from TASK_QUEUE.md.

Now CURRENT_STATUS.md#tail500: we have a large section with many updates and active tasks. Need to extract pending tasks from that.

Let's parse CURRENT_STATUS.md tail portion (the provided text). It includes sections: "最後更新：2026-09-24 A1午後巡查（remote cloud）｜系統今日活躍：A0 共 9 commits（msg 6059-6084，亞航 prebook 失敗 + Chrome history 安全修補 + SOP 看過才能說 + #59 leg 層實查 + 0050 雷達 + 小台/微台價差）；無 A2-A8 commit；NEEDS_REVIEW 10 tasks ~1989h；T-A7-001 Phase 3 第68天延誤；CRITICAL: T-A4-002 ~4221h / T-IOS-KOL-001 ~2709h / A6 ~2244h；A8 ~2524h；無新異常｜完整歷史存於 `archive/CURRENT_STATUS_2026-04-11_full.md`"

Then branch side old status line (not current). Then "## Active Task" etc.

We need to extract pending tasks from this section.

Let's locate task entries.

Under "## Active Task":

- `handoff/tasks/T-A6-HERMES-LINE-GYM-001.md` — `OPENROUTER_OUTPUT_PASS / OLLAMA_FALLBACK_DISABLED / KEY_ROTATION_REQUIRED / SHEETS_REDTEAM_BLOCKED / CHECKPOINT_SAVED / LIVE_NOT_DEPLOYED`
- Canonical flow：`docs/hermes-line-sheets-assistant-flow-v1.md`
- Machine contract：`config/hermes-line-sheets-assistant-v1.json`
- Validation receipt：`workbook/reviews/JOB-A6-HERMES-SHEETS-ASSISTANT-20260901/validation_report.md`
- Provider receipt：`workbook/reviews/JOB-A6-HERMES-OPENROUTER-REPLACEMENT-20260901/validation_receipt.md`
- Safety boundary：Hermes 只可建 `createQuoteShell`／`appendQuoteRevisionRequest` payload；live GAS deploy、真實 Sheet write、LINE 對客發訊、價格／菜單／檔期／飲食判斷與私密第三方外送均為 0。

Then "## Next Bounded Action"

- 依 checkpoint 先清除 legacy `scripts/apps-script/Code.gs` abandoned task diff，再完成 isolated GAS 的 schema、簽章、重放、乾淨表、冪等與 case↔quote lineage mocked tests，重寫 receipt 後才可重審。未授權前不得部署、建立真實客戶 Sheet、開啟 LINE sender或外送 raw LINE／客資；不再派真人標注、本機 Ollama 或 MLX training。

Then "## Resume Prompt"

- (description of what to do). Not a task.

Then "## Parallel Active Task — A8-FITNESS 華語樂齡節拍 MVP"

- Task Card：`handoff/tasks/T-A8-FITNESS-MVP-001.md`
- Status：`PRIVATE_MVP_RENDERED_UNVERIFIED / PT_REQUIRED`
- Durable heartbeat：每 30 分鐘在目前 Codex task 執行一個有檔案收據的 bounded action；不得新增 Suno 費用、再按 Create、上傳或公開。
- Current evidence：... (details)
- External boundary：YouTube 表單已填 `跟著動｜華語樂齡節拍`／`@跟著動樂齡節拍` 並顯示可用；final Create 尚未點，需 action-time confirmation。影片 upload／private／unlisted／public publication 全部 `NOT_AUTHORIZED`.

Then "### A8-FITNESS Next Bounded Action"

- 凍結安全修正版 exact MP4 hashes，從 encoded output 抽幀並產六份 fail-closed acceptance diagnostics；actual-audio human listen、目標裝置 readback、具名 PT hash-bound review 與 Owner publication gate 未齊前不得升 `QA_PASS`.

Then "### A8-FITNESS Resume Prompt"

- (description).

Then "## Parallel task pointer — A4 外燴預擺（2026-09-18）"

- Task Card: `handoff/tasks/T-A4-PRELAYOUT-SIMULATOR-001.md`; `FIRST_BATCH_VERIFIED / COLLECTION_INCOMPLETE / SIMULATOR_NOT_IMPLEMENTED`.
- Private entry: `/Users/pagemacmini/Documents/MAPLAB_外燴預擺/README.md`; 12 photos with per-photo notes, four candidate quote readbacks, original-preserving hash checks.
- Receipt: `reviews/PRELAYOUT-20260918/receipt.md`; six tests PASS; next bounded action and Resume Prompt are in the Task Card. This pointer does not replace the independent Hermes/A8 tasks above.

Then "## 系統版本" etc.

Then "## 最新事實核對" with many bullet points dated 2026-09-01, 2026-08-30, etc. These are completed items (they are facts). Not pending.

We need to extract pending tasks from this CURRENT_STATUS tail.

From Active Task: T-A6-HERMES-LINE-GYM-001.md is active, status indicates LIVE_NOT_DEPLOYED etc. So pending.

Next Bounded Action: tasks described: clear legacy diff, complete isolated GAS schema etc. That's pending.

Resume Prompt: not a task.

Parallel Active Task — A8-FITNESS: status PRIVATE_MVP_RENDERED_UNVERIFIED / PT_REQUIRED. So pending.

A8-FITNESS Next Bounded Action: pending.

A8-FITNESS Resume Prompt: not a task.

Parallel task pointer — A4 外燴預擺: status FIRST_BATCH_VERIFIED / COLLECTION_INCOMPLETE / SIMULATOR_NOT_IMPLEMENTED. So pending.

Thus we have pending tasks from CURRENT_STATUS.

Now we need to list pending tasks in a table with three columns: 待辦事項 / 文件中的狀態字樣(原文照抄) / 出自哪一個檔案.

We need to extract the task description (待辦事項) as appears in the file (maybe the task name). For each, we need to copy the status string exactly as appears in the file.

We must not exceed 600 characters total. That's tight. We need to be concise.

We must list all pending tasks from the three files. However, there are many tasks (maybe >20). 600 characters limit may force us to be selective? The instruction: "全文 600 字內". Means the entire output must be within 600 characters (including Chinese characters, punctuation, etc). That's very limited. We need to be extremely concise.

We need to list pending tasks; maybe we can list only the task names and statuses in a compact table.

But we must include all pending tasks? The instruction: "列出「還沒完成的待辦」". It implies list all pending tasks. But with 600 characters limit, we must be concise.

We can try to compress: each row maybe like "A6 LINE webhook 上線 / 🔴 CRITICAL（~275h） / TASK_QUEUE.md". That's about maybe 30-40 characters per row. If we have 15 rows, that's ~600. Might be okay.

Let's count approximate.

We need to include from TASK_QUEUE.md: Tier 1 (5 tasks), Tier 2 (6 tasks), FROZEN (8 tasks), Owner 自己接 (2 tasks), Owner 暫緩 (2 tasks). That's 23 tasks.

From CURRENT_STATUS: Active Task (1), Next Bounded Action (maybe 1), Parallel Active Task — A8-FITNESS (1), A8-FITNESS Next Bounded Action (1), Parallel task pointer — A4 外燴預擺 (1). That's 5 tasks.

Total ~28 tasks. If each row ~30 chars, that's 840 >600. Need to be even more concise.

Maybe we can combine multiple tasks per row? But format requires three columns per row; we could list multiple tasks separated by commas in the 待辦事項 column? Not sure if allowed. The instruction: "表格三欄:待辦事項 / 文件中的狀態字樣(原文照抄) / 出自哪一個檔案." Each row corresponds to one待辦事項. So each task separate row.

We need to reduce number of tasks listed. Perhaps many tasks are actually completed or not pending? Let's re-evaluate: Some tasks may be marked as completed elsewhere? But we only have the provided files; we must rely on status strings. If status indicates not done (like 🔴, ⏸️, 🔲, etc) we treat as pending. If status indicates completed (like ✅, 🟢 maybe not completed but ready). The 🟢 in Tier 1 for A2廣告現況巡查 + 關鍵字/行銷規劃: status "🟢 不再等 MCP token". That suggests it's not waiting for token, but hasn't been executed? The description says "走 ... 唯讀巡查...". It might be ready to run but not yet done. We'll treat as pending.

But maybe we can exclude tasks that have status indicating they are done (like ✅). In TASK_QUEUE.md, there is a section "✅ 本階段已交付（近期）" which lists completed items; we should not list those.

Thus we only list tasks with status not showing completion.

Now we need to be concise: maybe we can abbreviate task names heavily but must copy exact status string.

We can maybe list only the task identifiers (like T-A6-001) as 待辦事項? But we need to copy the task description as appears in file; we could use the task name column from the table (the first column includes the bold task name and ID). That is the 待辦事項.

We can copy that exactly; it's not too long.

Let's extract each task's first column content (including the bold formatting? We should copy as appears, but we can omit markdown bold? The instruction says "文件中的狀態字樣(原文照抄)" only for status column; for 待辦事項 we can copy as appears but maybe we can keep the plain text without markdown? Safer to copy the text as appears, but we can drop the ** markdown to keep plain? The instruction: "待辦事項 / 文件中的狀態字樣(原文照抄) / 出自哪一個檔案." It doesn't forbid modifying the task name? It says "待辦事項" column, not specifying must be exact copy. However to be safe, we can copy the task name as appears without markdown formatting (since the markdown is not part of the content). The file shows "**A6 LINE webhook 上線** — T-A6-001". The plain text is "A6 LINE webhook 上線 — T-A6-001". We'll use that.

Now we need to produce output within 600 characters. Let's attempt to list maybe 15 most critical tasks? But risk missing.

Maybe we can note that many tasks are similar across files; but we must list all.

Let's calculate approximate characters if we list each task as: "A6 LINE webhook 上線 — T-A6-001 / 🔴 CRITICAL（~275h） / TASK_QUEUE.md". Count characters:

Chinese characters count as 1 each. Let's approximate:

"A6 LINE webhook 上線 — T-A6-001" = length: "A6"2 + space1 + "LINE"4 + space1 + "webhook"7 + space1 + "上線"2 + space1 + "—"1 (maybe dash) + space1 + "T-A6-001"8 = total ~2+1+4+1+7+1+2+1+1+1+8 = 29? Actually let's count precisely: "A6"(2) + space(1)=3, "LINE"(4)=7, space=8, "webhook"(7)=15, space=16, "上線"(2)=18, space=19, "—"(1)=20, space=21, "T-A6-001"(8)=29. So ~29.

Status: "🔴 CRITICAL（~275h）" includes emoji (1 char?) plus text. Let's count: "🔴"(1) + space(1)=2, "CRITICAL"(8)=10, "（"(1)=11, "~275h"(5)=16, "）"(1)=17. So ~17.

Then space, slash, space: " / " =3.

Then filename: "TASK_QUEUE.md" length 13 (including dot). So total per row approx 29+1+3+17+3+13 = 66? Wait we need to add spaces: format: task / status / file. So we have task string, space, slash, space, status string, space, slash, space, file string. So add 2 spaces around each slash? Actually we wrote " / " includes spaces. So between task and first slash we have space then slash then space = 3. Between status and second slash similarly 3. So total = len(task) + 3 + len(status) + 3 + len(file). So 29+3+17+3+13 = 65.

Thus each row ~65 characters. 28 rows => 1820 >600. Too many.

We need to reduce number of rows drastically. Perhaps we can only list tasks that are explicitly marked as pending with a specific status like "
