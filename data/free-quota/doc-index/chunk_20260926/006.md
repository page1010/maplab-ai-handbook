> 草稿未審(free-quota 班表 20260926,model=nvidia/nemotron-3-super-120b-a12b:free,item=AGENT_STARTUP_PROTOCOL.md#L1-60)。翻譯/生成稿:不得新增事實,上線前必經人審。

We need to produce output: first line "來源:" then the exact line from the source: "----- 檔案:AGENT_STARTUP_PROTOCOL.md#L1-60 -----". Then a table with four columns: 條目標題 / 這一段在講什麼(30字內) / 關鍵字(3到6個,逗號分隔) / 對應行號範圍(用來源那一行的範圍,無法細分就照抄).

We need to split the segment into searchable index entries. The segment includes multiple sections: maybe we break into logical parts: e.g., "規則 6：經驗回寫", "Step 0. 企業文化與測試 receipt（冷啟動硬規則）", "啟動流程（7 步驟）", "Step 1. 讀 CURRENT_STATUS.md（最高優先）", etc. But we need to follow the instruction: "把這一段拆成可搜尋的索引條目,讓其他人不必整份重讀就能找到需要的那幾行." So we need to create entries for each distinct topic.

Given the segment includes many sub-sections: The header, then "規則 6：經驗回寫", then "## Step 0. 企業文化與測試 receipt（冷啟動硬規則）", then "### 啟動流程（7 步驟）" with steps 1-5.

We could make entries for each heading.

We need to keep each entry's description within 30 Chinese characters. Keywords 3-6, comma-separated.

The line range: we cannot subdivide further because we don't have exact line numbers for each sub-section; we only have overall range L1-60. So we must use that same range for each entry (or we could attempt to approximate but instruction says: "對應行號範圍(用來源那一行的範圍,無法細分就照抄)". So we just copy the source line range for each entry.

Then after table, we need a line "可執行的規定:" then list sentences that contain "必須/不得/禁止/一律". We need to extract those from the segment.

Let's scan the segment for those words.

From the segment:

- "所有 Agent 開始任務前，必須依序完成以下步驟。" (必須)
- "核心原則：先讀外部記憶，再開始工作。不准依賴聊天上下文判斷專案狀態。" (不准)
- Under 規則 6：經驗回寫: bullet points, not containing required words maybe.
- "不回寫 = 經驗只存在對話裡 = 對話結束就消失 = 下一個 Agent 重新摸索。" no required.
- Step 0: "開工前先讀 `docs/company-values.md`。這不是參考文件，是所有 agent 的企業文化契約。" no.
- "本輪只要會改程式、排程、owner-facing 訊息、Telegram/LINE/Chrome/WordPress/Sheets 行為，Startup Check 必須先寫：" (必須)
- Then bullet list: "- 預計測什麼：unit test / syntax check / live preview / readback / smoke test / screenshot QA。" no required.
- "- 測試 receipt 寫在哪裡：review bundle、validation report、task card、CURRENT_STATUS 或 handoff checkpoint。" no.
- "收尾前必須做到：" (必須)
- Then bullet: "- 跑最小可證明測試。" no.
- "- 把測試結果落檔。" no.
- "- Final 回覆列出 `Tests run`。" no.
- "> 有寫但沒測，等於沒完成；有測但沒 receipt，等於下一個 session 無法信任。" no.

- Step 1: "這是唯一最新狀態入口。確認：系統版本、當前 Phase、進行中任務、Blockers、Source of Truth 文件清單。" no.
- "> 若其他文件與 CURRENT_STATUS.md 衝突，以 CURRENT_STATUS.md 為準。" no required words.

- Step 2: no.

- Step 3: no.

- Step 4: no.

- Step 5: "若讀完路由表並以本機搜尋仍找不到精確 SOP／路徑／交接產物，先依 `config/notebooklm/maplab-project-brain-router.json` 問 MAPLAB Project Brain。沒有瀏覽器能力的地端模型先讀 `workbook/notebooklm/maplab-project-brain/maplab-sop-router.md`；不得因 NotebookLM 回答就跳過 live state、Task Card 或 receipt 驗證。" (不得)

- "Superpowers 規則**：
- **必拿**：skills/task-progress-guide.md — 所有任務都必須讀，不可跳過" (必須, 不可)
- "**必讀**：skills/session-lifecycle/SKILL.md — session 開關、Chrome tab 清理、禁止 keep-awake，全角色共用" (禁止)
- "Agent 產出的文字（commit message、Task Card、CHANGELOG）必須由 Agent 自己撰寫" (必須)

Thus we have many sentences with required words.

We need to list each sentence exactly as in the segment, original wording, that contains 必須/不得/禁止/一律.

We must copy them exactly, preserving punctuation.

Let's extract them:

1. "所有 Agent 開始任務前，必須依序完成以下步驟。"

2. "不准依賴聊天上下文判斷專案狀態。" (contains 不准, which is similar to 不得? The rule says list sentences that contain "必須/不得/禁止/一律". "不准" is not exactly those words, but maybe it's considered similar to 不得? The instruction says only those exact words: 必須/不得/禁止/一律. "不准" is not in list. So we should not include it unless it contains one of those exact strings. "不准" does not contain 不得, 不得 is two characters. So exclude.

3. "本輪只要會改程式、排程、owner-facing 訊息、Telegram/LINE/Chrome/WordPress/Sheets 行為，Startup Check 必須先寫：" contains 必須.

4. "收尾前必須做到：" contains 必須.

5. "> 有寫但沒測，等於沒完成；有測但沒 receipt，等於下一個 session 無法信任。" no.

6. "> 若其他文件與 CURRENT_STATUS.md 衝突，以 CURRENT_STATUS.md 為準。" no.

7. "不得因 NotebookLM 回答就跳過 live state、Task Card 或 receipt 驗證。" contains 不得.

8. "**必拿**：skills/task-progress-guide.md — 所有任務都必須讀，不可跳過" contains 必須 and 不可 (不可 is not one of the four, but we only need sentences that contain any of the four; this sentence contains 必須, so include.

9. "**必讀**：skills/session-lifecycle/SKILL.md — session 開關、Chrome tab 清理、禁止 keep-awake，全角色共用" contains 禁止.

10. "Agent 產出的文字（commit message、Task Card、CHANGELOG）必須由 Agent 自己撰寫" contains 必須.

Any others? Let's re-check.

Also there is "必須先寫：" already counted.

Also maybe "必須" appears elsewhere like "必須先寫：" we have.

Also "必須" appears in "必須先寫：" we captured.

Also "必須" appears in "必須讀" maybe? Actually we have "**必讀**：..." but that's not 必須.

Also there is "必須" in "必須先寫：" we captured.

Also "必須" appears in "必須先寫：" done.

Also "必須" appears in "必須先寫：" again.

Check also "必須" in "必須先寫：" yes.

Also "必須" appears in "必須先寫：" done.

Also "必須" appears in "必須先寫：" done.

Now also check for "一律": none.

Now also check for "禁止": we have "禁止 keep-awake".

Now also check for "不得": we have "不得因 NotebookLM 回答就跳過 live state、Task Card 或 receipt 驗證。"

Now also check for "必須" in "必須先寫：" captured.

Also "必須" in "收尾前必須做到：" captured.

Also "必須" in "所有 Agent 開始任務前，必須依序完成以下步驟。" captured.

Also "必須" in "**必拿**：... 所有任務都必須讀，不可跳過" captured.

Also "必須" in "Agent 產出的文字（commit message、Task Card、CHANGELOG）必須由 Agent 自己撰寫" captured.

Also maybe "必須" appears in "必須先寫：" we already have.

Now ensure we copy the sentences exactly as they appear, including surrounding punctuation and spaces.

Let's extract each sentence with its exact text from the segment.

We'll need to locate them in the segment.

Segment text:

```
# AGENT_STARTUP_PROTOCOL.md — 接手前必讀 SOP
**所有 Agent 開始任務前，必須依序完成以下步驟。**
這份文件的目的是解決「每個 Agent 一開始沒有大局觀」和「分頁斷線後記憶歸零」的問題。

> **核心原則：先讀外部記憶，再開始工作。不准依賴聊天上下文判斷專案狀態。**


### 規則 6：經驗回寫
任務結束（或子任務結束）時，檢查是否有值得記錄的經驗：
- 成功路徑 → 更新 projects/maplab-playbook.md 對應 SECTION 的「最短路徑」
- 工具選擇 → 更新對應 skills/ 技能書的工具比較表
- 新踩的坑 → 新增 skills/experience-log.md 條目
- 什麼都沒有 → 在 Handoff Checkpoint 寫「同現有流程，無新發現」

> 不回寫 = 經驗只存在對話裡 = 對話結束就消失 = 下一個 Agent 重新摸索。
---

## Step 0. 企業文化與測試 receipt（冷啟動硬規則）

開工前先讀 `docs/company-values.md`。這不是參考文件，是所有 agent 的企業文化契約。

本輪只要會改程式、排程、owner-facing 訊息、Telegram/LINE/Chrome/WordPress/Sheets 行為，Startup Check 必須先寫：

- 預計測什麼：unit test / syntax check / live preview / readback / smoke test / screenshot QA。
- 測試 receipt 寫在哪裡：review bundle、validation report、task card、CURRENT_STATUS 或 handoff checkpoint。

收尾前必須做到：

- 跑最小可證明測試。
- 把測試結果落檔。
- Final 回覆列出 `Tests run`。

> 有寫但沒測，等於沒完成；有測但沒 receipt，等於下一個 session 無法信任。

---

## 啟動流程（7 步驟）

### Step 1. 讀 CURRENT_STATUS.md（最高優先）
這是唯一最新狀態入口。確認：系統版本、當前 Phase、進行中任務、Blockers、Source of Truth 文件清單。
> 若其他文件與 CURRENT_STATUS.md 衝突，以 CURRENT_STATUS.md 為準。

### Step 2. 讀 handoff/tasks/ Task Card
確認：你的任務是什麼、上一個 Agent 做到哪、下一步是什麼、Blockers。

### Step 3. 讀 AGENT_RULES.md
確認：自己的角色編號（A1-A7）、負責範圍、禁止事項。

### Step 4. 讀對應的 Task Card（handoff/tasks/T-xxx.md）
如果你要接手一個進行中任務，讀它的 Task Card 確認：上一個 Agent 做到哪、下一步是什麼、Blockers。

### Step 5. 讀 skills/superpowers-guide.md 路由表 + 必拿技能
查「任務類型 → 建議預讀技能書」，選擇最適合當前任務的技能書。

若讀完路由表並以本機搜尋仍找不到精確 SOP／路徑／交接產物，先依 `config/notebooklm/maplab-project-brain-router.json` 問 MAPLAB Project Brain。沒有瀏覽器能力的地端模型先讀 `workbook/notebooklm/maplab-project-brain/maplab-sop-router.md`；不得因 NotebookLM 回答就跳過 live state、Task Card 或 receipt 驗證。

**Superpowers 規則**：
- **必拿**：skills/task-progress-guide.md — 所有任務都必須讀，不可跳過
- **必讀**：skills/session-lifecycle/SKILL.md — session 開關、Chrome tab 清理、禁止 keep-awake，全角色共用
- Agent 產出的文字（commit message、Task Card、CHANGELOG）必須由 Agent 自己撰寫
```

Now extract sentences.

1. "**所有 Agent 開始任務前，必須依序完成以下步驟。**"
   Note includes the surrounding **. We need to copy exactly as appears: including the double asterisks? The sentence includes them as part of markdown bold. The instruction: "原文照抄". So we should copy exactly: "**所有 Agent 開始任務前，必須依序完成以下步驟。**"

2. "本輪只要會改程式、排程、owner-facing 訊息、Telegram/LINE/Chrome/WordPress/Sheets 行為，Startup Check 必須先寫：" appears after a line break. The exact text: "本輪只要會改程式、排程、owner-facing 訊息、Telegram/LINE/Chrome/WordPress/Sheets 行為，Startup Check 必須先寫："

Check punctuation: there is a colon (全形?) It's a fullwidth colon after "寫". In the text: "Startup Check 必須先寫：" yes.

3. "收尾前必須做到：" exact: "收尾前必須做到："

4. "**必拿**：skills/task-progress-guide.md — 所有任務都必須讀，不可跳過" exact: "**必拿**：skills/task-progress-guide.md — 所有任務都必須讀，不可跳過"

5. "**必讀**：skills/session-lifecycle/SKILL.md — session 開關、Chrome tab 清理、禁止 keep-awake，全角色共用" exact: "**必讀**：skills/session-lifecycle/SKILL.md — session 開關、Chrome tab 清理、禁止 keep-awake，全角色共用"

6. "Agent 產出的文字（commit message、Task Card、CHANGELOG）必須由 Agent 自己撰寫" exact: "Agent 產出的文字（commit message、Task Card、CHANGELOG）必須由 Agent 自己撰寫"

7. "不得因 NotebookLM 回答就跳過 live state、Task Card 或 receipt 驗證。" exact: "不得因 NotebookLM 回答就跳過 live state、Task Card 或 receipt 驗證。"

Check if there are any other sentences with 必須/不得/禁止/一律.

Also there is "不准" not included.

Also maybe "必須" appears in "必須先寫：" we captured.

Also "必須" appears in "必須做到：" captured.

Also "必須" appears in "必須讀" is part of "**必讀**：" but that's not the word 必須, it's 必讀, not included.

Now we have list of 7 sentences.

Now we need to output after table: a line "可執行的規定:" then each sentence on its own line? The instruction: "只列這一段裡寫成「必須/不得/禁止/一律」的句子,原文照抄,沒有就寫「本段無」." So we list each sentence, likely each on new line.

Now we need to create index entries.

We need to decide entry titles and summary (30 chars). Let's think of distinct sections:

- "規則 6：經驗回寫"
- "Step 0. 企業文化與測試 receipt（冷啟動硬規則）"
- "啟動流程（7 步驟）" maybe we can break each step but we can keep as one entry for the whole流程? However we need to make searchable; better to break each step.

But we have limited time; we can create entries for each major heading:

1. 規則 6：經驗回寫
2. Step 0. 企業文化與測試 receipt（冷啟動硬規則）
3. Step 1. 讀 CURRENT_STATUS.md（最高優先）
4. Step 2. 讀 handoff/tasks/ Task Card
5. Step 3. 讀 AGENT_RULES.md
6. Step 4. 讀對應的 Task Card（handoff/tasks/T-xxx.md）
7. Step 5. 讀 skills/superpowers-guide.md 路由表 + 必拿技能
8. Superpowers 規則 (必拿, 必讀, Agent 產出文字)

Alternatively combine steps 1-5 into one entry "啟動流程（7 步驟）". But we need to ensure each entry's description is within 30 Chinese characters.

Let's decide to create entries for each of the bolded sections we can see: "規則 6：經驗回寫", "Step 0. 企業文化與測試 receipt（冷啟動硬規則）", "啟動流程（7 步驟）" (maybe summarise), then each step under it.

But the instruction: "把這一段拆成可搜尋的索引條目,讓其他人不必整份重讀就能找到需要的那幾行." So we can split by logical parts.

I'll produce entries:

- 條目標題: 規則 6：經驗回寫
  概述: 任務結束時記錄經驗的規則
  關鍵字: 經驗回寫,成功路徑,工具選擇,新踩的坑,Handoff Checkpoint
  行號範圍: L1-60

But need description within 30 Chinese characters. Let's count: "任務結束時記錄經驗的規則" that's 12 characters.

Ok.

- 條目標題: Step 0. 企業文化與測試 receipt（冷啟動硬規則）
  概述: 開工前必須讀公司文化並寫測試計畫與收尾驗證
  Count: maybe 28? Let's count: 開工前必須讀公司文化並寫測試計畫與收尾驗證 => characters: 開(1)工2 前3 必4 須5 讀6 公7 司8 文9 化10 並11 寫12 測13 試14 計15 畫16 與17 收18 尾19 驗20 證21 => 21 characters. Good.

  Keywords: 公司文化,測試receipt,Startup Check,收尾驗證

- 條目標題: Step 1. 讀 CURRENT_STATUS.md（最高優先）
  概述: 確認系統版本、階段、任務、Blockers、Source of Truth
  Count: maybe 20.

  Keywords: CURRENT_STATUS,系統版本,Phase,Blockers,Source of Truth

- 條目標題: Step 2. 讀 handoff/tasks/ Task Card
  概述: 確認任務內容、上一 Agent 進度、下一步、Blockers
  Count: maybe 22.

  Keywords: Task Card,任務內容,上一 Agent,下一步,Blockers

- 條目標題: Step 3. 讀 AGENT_RULES.md
  概述: 確認角色編號、負責範圍、禁止事項
  Count: maybe 18.

  Keywords: AGENT_RULES,角色編號,負責範圍,禁止事項

- 條目標題: Step 4. 讀對應的 Task Card（handoff/tasks/T-xxx.md）
  概述: 確認進行中任務的進度與下一步
  Count: maybe 20.

  Keywords: Task Card,進行中任務,進度,下一步

- 條目標題: Step 5. 讀 skills/superpowers-guide.md 路由表 + 必拿技能
  概述: 查任務類型→預讀技能書，若找不到則查詢 Project Brain
  Count: maybe 30? Let's count: 查任務類型→預讀技能書，若找不到則查詢 Project Brain => characters: 查1 任2 務3 類4 型5 →6 預7 讀8 技9 能10 書11 ，12 若13 找14 不15 到16 則17 查18 詢19 空20 P21 r22 o23 j24 e25 c26 t27  28 B29 r30 a31 i32 n33 => 33 maybe exceed. Let's shorten: "查任務類型預讀技能書，找不到則問 Project Brain". Count: 查1 任2 務3 類4 型5 預6 讀7 技8 能9 書10 ，11 找12 不13 到14 則15 問16 空17 P18 r19 o20 j21 e22 c23 t24  25 B26 r27 a28 i29 n30 => 30 exactly? Actually we have spaces? Let's just keep within 30 Chinese characters ignoring spaces and punctuation? The instruction says 30字內, likely counting characters. Let's aim <=30.

We'll use: "查任務類型預讀技能書，找不到則問 Project Brain". Count: 查(1)任2務3類4型5預6讀7技8能9書10，11找12不13到14則15問16空17P18r19o20j21e22c23t24 25B26r27a28i29n30 => 30 exactly (including the space after t?). Might be okay.

Keywords: superpowers-guide,任務類型,技能書,Project Brain

- 條目標題: Superpowers 規則
  概述: 必拿技能書、必讀session生命週期、Agent 產出文字必須自行撰寫
  Count: maybe 30? Let's count: 必拿技能書、必讀session生命週期、Agent 產出文字必須自行撰寫 => characters: 必1 拿2 技3 能4 書5 ，6 必7 讀8 s9 e10 s11 s12 i13 o14 n15 生16 命17 週18 期19 ，20 A21 g22 e23 n24 t25 空26
